je_organised_crime_system = {
	icon = "gfx/interface/icons/invention_icons/law_enforcement.dds"

	group = je_group_internal_affairs

	is_shown_when_inactive = {
		NOR = {
			AND = {
				OR = {
					has_law_or_variant = law_type:law_lenient_policing
					has_law_or_variant = law_type:law_very_low_anti_corruption
				}
				institution_investment_level = {
					institution = institution_police
					value >= 5
				}
			}
			AND = {
				OR = {
					has_law_or_variant = law_type:law_balanced_policing
					has_law_or_variant = law_type:law_low_anti_corruption
				}
				institution_investment_level = {
					institution = institution_police
					value >= 4
				}
			}
			AND = {
				OR = {
					has_law_or_variant = law_type:law_strict_policing
					has_law_or_variant = law_type:law_medium_anti_corruption
				}
				institution_investment_level = {
					institution = institution_police
					value >= 3
				}
			}
			AND = {
				OR = {
					has_law_or_variant = law_type:law_draconian_policing
					has_law_or_variant = law_type:law_high_anti_corruption
				}
				institution_investment_level = {
					institution = institution_police
					value >= 2
				}
			}
			AND = {
				has_law_or_variant = law_type:law_draconian_policing
				has_law_or_variant = law_type:law_very_high_anti_corruption
				institution_investment_level = {
					institution = institution_police
					value >= 1
				}
			}
		}
	}

	possible = {
		
	}

	immediate = {
		
	}
	
	complete = {
		OR = {
			AND = {
				OR = {
					has_law_or_variant = law_type:law_lenient_policing
					has_law_or_variant = law_type:law_very_low_anti_corruption
				}
				institution_investment_level = {
					institution = institution_police
					value >= 5
				}
			}
			AND = {
				OR = {
					has_law_or_variant = law_type:law_balanced_policing
					has_law_or_variant = law_type:law_low_anti_corruption
				}
				institution_investment_level = {
					institution = institution_police
					value >= 4
				}
			}
			AND = {
				OR = {
					has_law_or_variant = law_type:law_strict_policing
					has_law_or_variant = law_type:law_medium_anti_corruption
				}
				institution_investment_level = {
					institution = institution_police
					value >= 3
				}
			}
			AND = {
				OR = {
					has_law_or_variant = law_type:law_draconian_policing
					has_law_or_variant = law_type:law_high_anti_corruption
				}
				institution_investment_level = {
					institution = institution_police
					value >= 2
				}
			}
			AND = {
				has_law_or_variant = law_type:law_draconian_policing
				has_law_or_variant = law_type:law_very_high_anti_corruption
				institution_investment_level = {
					institution = institution_police
					value >= 1
				}
			}
		}
	}
	
	on_complete = {
		
		custom_tooltip = {
			text = building_opium_plantation_destroy_tt
			every_scope_state = {
				if = {
					limit = {
						has_building = building_opium_plantation
					}
					remove_building = building_opium_plantation
				}
			}
		}
		custom_tooltip = {
			text = building_vineyard_destroy_tt
			every_scope_state = {
				if = {
					limit = {
						has_building = building_vineyard
					}
					remove_building = building_vineyard
				}
			}
		}
		custom_tooltip = {
			text = building_liquor_factory_destroy_tt
			every_scope_state = {
				if = {
					limit = {
						has_building = building_liquor_factory
					}
					remove_building = building_liquor_factory
				}
			}
		}
		custom_tooltip = {
			text = building_tobacco_plantation_destroy_tt
			every_scope_state = {
				if = {
					limit = {
						has_building = building_tobacco_plantation
					}
					remove_building = building_tobacco_plantation
				}
			}
		}
		
	}
	
	# zero or more static modifiers that will be applied to the Journal Entry when it activates, where they will propagate to the country
	modifiers_while_active = {
		organised_crime_system
	}

	weight = 100

	should_be_pinned_by_default_uninvolved_or_context = no

	transferable = no

	progressbar = no
}