# This play is only used as a 'default diplomatic play' for certain triggers etc, and potentially for scripted content
# It can never actually be launched in the normal way
dp_default_treaty_article = {
	# This defaults to a standard money transfer article
	war_goal = enforce_treaty_article

	texture = "gfx/interface/icons/war_goals/enforce_treaty_article.dds"

	add_infamy_for_starting_target_wargoals = yes

	selectable_in_lens = {
		always = no
	}

	possible = {
		always = no
	}

	ai_acceptance_max = 100

	on_weekly_pulse = {}

	on_war_begins = {}

	on_war_end = {}
}

dp_regime_change = {
	war_goal = regime_change
	
	texture = "gfx/interface/icons/war_goals/regime_change.dds"

	possible = {
	
		NOT = { is_country_type = decentralized }
		scope:target_country = {
			NOT = { is_country_type = decentralized }
		}
		
		scope:target_country = { is_subject = no }
		
		aggressive_diplomatic_plays_permitted = yes

		diplo_play_war_check_cwe = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe_ideology_bypass = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	on_demand_accepted = {
		scope:target_country = { copy_laws = scope:initiator }
	}

	ai_acceptance_max = 100 on_weekly_pulse = {}

	on_war_begins = {}

	on_war_end = {}
	
}

dp_ban_slavery = {
	war_goal = ban_slavery
	
	texture = "gfx/interface/icons/war_goals/ban_slavery.dds"

	add_infamy_for_starting_target_wargoals = yes

	possible = {
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		NOT = { is_subject_of = scope:target_country }	
		scope:target_country = {
			NOT = { is_country_type = decentralized }
			NOT = { is_subject_of = root }
		}

		diplo_play_war_check_cwe = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}

	ai_acceptance_max = 100 on_weekly_pulse = {}

	on_war_begins = {}

	on_war_end = {}
}

dp_conquer_state = {
	war_goal = conquer_state
	
	texture = "gfx/interface/icons/war_goals/conquer_state.dds"
	
	possible = {
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		NOT = { is_subject_of = scope:target_country }
		scope:target_country = {
			NOT = { is_country_type = decentralized }
			NOT = { is_subject_of = root }
		}	

		diplo_play_war_check_cwe = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_conquer_state_historical = {
	requires_interest_marker = no
	initiator_can_add_war_goals = no
	target_can_add_war_goals = no
	
	war_goal = conquer_state
	
	texture = "gfx/interface/icons/war_goals/conquer_state.dds"
	
	selectable_in_lens = {
		always = no
	}
	
	possible = {
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		NOT = { is_subject_of = scope:target_country }
		scope:target_country = {
			NOT = { is_country_type = decentralized }
			NOT = { is_subject_of = root }
		}	

		diplo_play_war_check_cwe = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_return_state = {
	requires_interest_marker = no

	war_goal = return_state
	
	texture = "gfx/interface/icons/war_goals/return_state.dds"
	
	possible = {
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		NOT = { is_subject_of = scope:target_country }	
		scope:target_country = {
			NOT = { is_country_type = decentralized }
		}

		diplo_play_war_check_cwe = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_return_state_historical = {
	war_goal = return_state
	requires_interest_marker = no
	initiator_can_add_war_goals = no
	target_can_add_war_goals = no
	
	texture = "gfx/interface/icons/war_goals/return_state.dds"
	
	selectable_in_lens = {
		always = no
	}
	
	possible = {
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		NOT = { is_subject_of = scope:target_country }	
		scope:target_country = {
			NOT = { is_country_type = decentralized }
			NOT = { is_subject_of = root }
		}

		diplo_play_war_check_cwe = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_seize_condominiums = {
	war_goal = seize_condominiums
	
	texture = "gfx/interface/icons/war_goals/conquer_state.dds"
	
	possible = {	
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		NOT = { is_subject_of = scope:target_country }	
		scope:target_country = {
			NOT = { is_country_type = decentralized }
			NOT = { is_subject_of = root }
		}
		
		custom_tooltip = {
			text = "THIS_OWNS_CONDOMINIUMS"
			scope:target_country = {
				any_scope_diplomatic_pact = {
					is_diplomatic_action_type = condominium
					first_country = scope:target_country
				}
			}
		}

		diplo_play_war_check_cwe = yes
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_liberate_subject = {
	war_goal = liberate_subject

	texture = "gfx/interface/icons/war_goals/liberate_subject.dds"

	possible = {
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		NOT = { is_subject_of = scope:target_country }	
		scope:target_country = {
			NOT = { is_country_type = decentralized }
			is_subject = yes
			NOT = { is_subject_of = root }
		}

		diplo_play_war_check_cwe = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}

	ai_acceptance_max = 100 on_weekly_pulse = {}

	on_war_begins = {}

	on_war_end = {}
}

dp_liberate_country = {
	war_goal = liberate_country

	texture = "gfx/interface/icons/war_goals/liberate_country.dds"

	possible = {
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		NOT = { is_subject_of = scope:target_country }	
		diplomatic_play_has_no_relations_blockers = yes
		
		diplo_play_war_check_cwe = yes
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}

	ai_acceptance_max = 100 on_weekly_pulse = {}

	on_war_begins = {}

	on_war_end = {}
}

dp_liberate_country_as_subject = {
	war_goal = liberate_country_as_subject

	texture = "gfx/interface/icons/lens_toolbar_icons/puppet.dds"

	possible = {
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		NOT = { is_subject_of = scope:target_country }	
		diplomatic_play_has_no_relations_blockers = yes
		
		diplo_play_war_check_cwe = yes
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}

	ai_acceptance_max = 100 on_weekly_pulse = {}

	on_war_begins = {}

	on_war_end = {}
}

dp_liberate_country_as_mandate = {
	war_goal = liberate_country_as_mandate

	texture = "gfx/interface/icons/lens_toolbar_icons/chartered_company.dds"

	possible = {
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		NOT = { is_subject_of = scope:target_country }	
		diplomatic_play_has_no_relations_blockers = yes
		
		diplo_play_war_check_cwe = yes
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}

	ai_acceptance_max = 100 on_weekly_pulse = {}

	on_war_begins = {}

	on_war_end = {}
}

dp_independence = {
	requires_interest_marker = no
	initiator_can_add_war_goals = no

	war_goal = independence

	texture = "gfx/interface/icons/war_goals/independence.dds"
	
	confirmation_sound = "event:/SFX/UI/SubjectInteractions/subject_button_small_independence"

	possible = {
		NOT = { is_country_type = decentralized }
		
		#Edging closer to war
		custom_tooltip = {
			text = "REQUEST_AUTONOMY_RECENTLY_DENIED"
			any_diplomatic_catalyst = {
				is_diplomatic_catalyst_type = catalyst_autonomy_increase_denied
				target = scope:target_country
			}
		}
		
		#Bad relations with TARGET
		custom_tooltip = {
			text = DIPLO_ACTION_RELATIONS_POOR_OR_LESS
			scope:target_country.relations:root <= relations_threshold:poor
		}
		
		#THIS has some army
		NOT = { has_law_or_variant = law_type:law_peasant_levies }
		
		#Domestic pressure for independence
		OR = { 
		
			#Overlord and Subject rulers are incompatible
			ruler ?= {
				culture = { culture_is_discriminated_in = scope:target_country }
			}
			AND = {
				scope:target_country = { 
					ruler ?= {
						OR = {
							has_ideology = ideology:ideology_autocratic
							has_ideology = ideology:ideology_power_hungry
						}
					}
				}
				ROOT = {
					ruler ?= {
						OR = {
							has_ideology = ideology:ideology_democratic
							has_ideology = ideology:ideology_liberal
						}
					}
				}
			}
			ruler ?= {
				OR = {
					has_ideology = ideology:ideology_autocratic
					has_ideology = ideology:ideology_power_hungry
				}
			}
		
		}
		
		#Parliament and Cabinet approves
		government_has_legislative_simple_majority = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}

	ai_acceptance_max = 100 on_weekly_pulse = {}

	on_war_begins = {}

	on_war_end = {}
}

dp_independence_condominium = {
	requires_interest_marker = no
	initiator_can_add_war_goals = no

	war_goal = independence_condominium

	texture = "gfx/interface/icons/war_goals/independence.dds"
	
	confirmation_sound = "event:/SFX/UI/SubjectInteractions/subject_button_small_independence"

	possible = {
		
		#Condominium only
		has_modifier = condominium_modifier
		scope:target_country = { has_diplomatic_pact = { who = ROOT type = condominium is_initiator = yes } }
	
		NOT = { is_country_type = decentralized }
		
		#Bad relations with TARGET
		custom_tooltip = {
			text = DIPLO_ACTION_RELATIONS_POOR_OR_LESS
			#scope:target_country.relations:root <= relations_threshold:poor
		}
		
		#THIS has some army
		NOT = { has_law_or_variant = law_type:law_peasant_levies }
		
		#Domestic pressure for independence
		OR = { 
		
			#Overlord and Subject rulers are incompatible
			ruler ?= {
				culture = { culture_is_discriminated_in = scope:target_country }
			}
			AND = {
				scope:target_country = { 
					ruler ?= {
						OR = {
							has_ideology = ideology:ideology_autocratic
							has_ideology = ideology:ideology_power_hungry
						}
					}
				}
				ROOT = {
					ruler ?= {
						OR = {
							has_ideology = ideology:ideology_democratic
							has_ideology = ideology:ideology_liberal
						}
					}
				}
			}
			ruler ?= {
				OR = {
					has_ideology = ideology:ideology_autocratic
					has_ideology = ideology:ideology_power_hungry
				}
			}
		
		}
		
		#Parliament and Cabinet approves
		#government_has_legislative_simple_majority = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}

	ai_acceptance_max = 100 on_weekly_pulse = {}

	on_war_begins = {}

	on_war_end = {}
}

dp_take_treaty_port = {
	
	war_goal = take_treaty_port
	
	texture = "gfx/interface/icons/war_goals/take_treaty_port.dds"
	
	add_infamy_for_starting_target_wargoals = yes
	
	possible = {
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		NOT = { is_subject_of = scope:target_country }
		scope:target_country = {
			NOT = { is_country_type = decentralized }
			NOT = { is_subject_of = root }
		}
		
		diplo_play_war_check_cwe = yes
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_leave_power_bloc = {
	requires_interest_marker = no

	war_goal = leave_power_bloc

	texture = "gfx/interface/icons/war_goals/leave_power_bloc.dds"

	possible = {
		NOT = { is_country_type = decentralized }
		diplo_play_war_check_cwe = yes
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}

	ai_acceptance_max = 100
	
	on_weekly_pulse = {}

	on_war_begins = {}

	on_war_end = {}
}

dp_increase_autonomy = {
	war_goal = increase_autonomy
	
	requires_interest_marker = no
	
	texture = "gfx/interface/icons/war_goals/increase_autonomy.dds"
	
	#Makes no sense in 21st century to declare war just to become a more autonomous puppet
	selectable_in_lens = {
		always = no
	}
	
	possible = {
	
		is_subject = yes	
		custom_tooltip = {
			text = "REQUEST_AUTONOMY_RECENTLY_DENIED"
			any_diplomatic_catalyst = {
				is_diplomatic_catalyst_type = catalyst_autonomy_increase_denied
				target = scope:target_country
			}
		}
		
		#THIS has some army
		NOT = { has_law_or_variant = law_type:law_peasant_levies }
		
		#Parliament and Cabinet approves
		government_has_legislative_simple_majority = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_reduce_autonomy = {
	war_goal = reduce_autonomy

	requires_interest_marker = no

	texture = "gfx/interface/icons/war_goals/reduce_autonomy.dds"

	possible = {
	
		NOR = {
			scope:target_country = { is_subject_type = subject_type_personal_union }
			scope:target_country = { is_subject_type = subject_type_protectorate }
		}
	
		diplo_play_war_check_cwe = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}

	on_weekly_pulse = {}

	on_war_begins = {}

	on_war_end = {}
}

dp_make_dominion = {
	
	war_goal = make_dominion
	
	texture = "gfx/interface/icons/war_goals/make_dominion.dds"
	
	possible = {
		aggressive_diplomatic_plays_permitted = yes	

		diplo_play_war_check_cwe = yes
		
		#THIS has higher rank than TARGET
		country_rank > scope:target_country.country_rank
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_make_mandate = {
	war_goal = make_mandate
	
	texture = "gfx/interface/icons/lens_toolbar_icons/chartered_company.dds"
	
	possible = {
	
		aggressive_diplomatic_plays_permitted = yes			

		diplo_play_war_check_cwe = yes
		
		#THIS has higher rank than TARGET
		country_rank > scope:target_country.country_rank
		
		#Can only make mandates out of non-sovereign subjects
		scope:target_country = {
			NOT = { is_sufficiently_independent = yes }
		}
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_make_puppet = {
	war_goal = make_protectorate
	
	texture = "gfx/interface/icons/war_goals/make_protectorate.dds"
	
	possible = {
		aggressive_diplomatic_plays_permitted = yes			

		diplo_play_war_check_cwe = yes
		
		#THIS has higher rank than TARGET
		country_rank > scope:target_country.country_rank
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_make_condominium = {
	war_goal = make_condominium
	
	texture = "gfx/interface/icons/lens_toolbar_icons/territory.dds"
	
	possible = {	
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		NOT = { is_subject_of = scope:target_country }	
		scope:target_country = {
			NOT = { is_country_type = decentralized }
			is_subject = yes
			NOT = { is_subject_of = root }
			NOT = { is_subject_type = subject_type_personal_union }
		}

		diplo_play_war_check_cwe = yes
		
		#THIS has higher rank than TARGET
		country_rank > scope:target_country.country_rank
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_transfer_subject = {
	war_goal = transfer_subject
	
	texture = "gfx/interface/icons/war_goals/transfer_subject.dds"
	
	possible = {	
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		NOT = { is_subject_of = scope:target_country }	
		scope:target_country = {
			NOT = { is_country_type = decentralized }
			is_subject = yes
			NOT = { is_subject_of = root }
			NOT = { is_subject_type = subject_type_personal_union }
		}

		diplo_play_war_check_cwe = yes
		
		#THIS has higher rank than TARGET
		country_rank > scope:target_country.country_rank
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_annex_subject = {
	war_goal = annex_country
	
	texture = "gfx/interface/icons/war_goals/annex_country.dds"
	
	confirmation_sound = "event:/SFX/UI/SubjectInteractions/subject_button_small_annex"
	
	possible = {
		NOT = { is_country_type = decentralized }
		scope:target_country = { NOT = { is_country_type = decentralized } }
		
		aggressive_diplomatic_plays_permitted = yes
		scope:target_country = {
			is_direct_subject_of = root
		}

		#THIS does not have restricted war rights
		NOR = { 
			has_law_or_variant = law_type:law_no_war
			has_law_or_variant = law_type:law_defensive_war
		}
		
		#THIS has some army
		NOT = { has_law_or_variant = law_type:law_peasant_levies }
		
		#Ruler is jingoistic OR is a dictatorship and has a war mongering IG OR have ideological differences
		OR = { 
		
			AND = { 
				exists = ruler
				ruler ?= {
					OR = {
						has_ideology = ideology:ideology_autocratic
						has_ideology = ideology:ideology_power_hungry
					}
				}
			}
			
			AND = {
				any_interest_group = {
					is_in_government = yes
					OR = { 
						is_powerful = yes
						has_modifier = cabinet_minister_ig
					}
					leader ?= { 
						OR = {
							has_ideology = ideology:ideology_autocratic
							has_ideology = ideology:ideology_power_hungry
						}
					}
				}
			}
			
			AND = {
				has_law_or_variant = law_type:law_HOS_monarch
				scope:target_country ?= { NOT = { has_law_or_variant = law_type:law_HOS_monarch } }
			}
			
			AND = {
				has_law_or_variant = law_type:law_HOS_president
				scope:target_country ?= { NOT = { has_law_or_variant = law_type:law_HOS_president } }
			}
			
			AND = {
				has_law_or_variant = law_type:law_HOS_religious_leader
				scope:target_country ?= { NOT = { has_law_or_variant = law_type:law_HOS_religious_leader } }
			}
			
			AND = {
				has_law_or_variant = law_type:law_HOS_nationalist_leader
				scope:target_country ?= { NOT = { has_law_or_variant = law_type:law_HOS_nationalist_leader } }
			}
			
			AND = {
				has_law_or_variant = law_type:law_HOS_communist_leader
				scope:target_country ?= { NOT = { has_law_or_variant = law_type:law_HOS_communist_leader } }
			}
		
		}
		
		#No non-aggression_pact with TARGET
		has_no_non_aggression_pact_with_target = yes
		
		#Parliament and Cabinet approves
		government_has_legislative_simple_majority = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {
		scope:initiator = {
			every_subject_or_below = {
				limit = {
					is_direct_subject_of = scope:initiator
					NOT = { this = scope:target_country }
				}
				add_liberty_desire = annex_subject_base_liberty_desire_change
				add_modifier = {
					name = modifier_has_annexed_another_subject
					months = 60
					is_decaying = no
				}
			}
		}
	}

	on_war_end = {}
}

dp_revolution = {
	requires_interest_marker = no
	enable_switch_sides = yes
	allow_negotiated_peace = no
	mirror_war_goal = yes
	initiator_can_add_war_goals = no
	target_can_add_war_goals = no
	add_infamy_for_starting_initiator_wargoals = no
	
	war_goal = annex_country
	
	texture = "gfx/interface/icons/war_goals/annex_country.dds"
	
	selectable_in_lens = {
		always = no
	}	
	
	possible = {
		scope:initiator = {
			is_revolutionary = yes
		}
		scope:initiator.civil_war_origin_country = scope:target_country
	}

	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			has_interventionist_fp = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:target_country = {
			capital = {
				region = {
					save_temporary_scope_as = target_capital_region
				}
			}
		}
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:target_capital_region
			has_interest_marker_in_region = scope:initiator_capital_region
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}

	on_war_begins = {}

	on_war_end = {}
}

dp_historical_revolution = {
	requires_interest_marker = no
	enable_switch_sides = yes
	allow_negotiated_peace = no
	mirror_war_goal = yes
	initiator_can_add_war_goals = no
	target_can_add_war_goals = no
	add_infamy_for_starting_initiator_wargoals = no
	
	war_goal = annex_country
	
	texture = "gfx/interface/icons/war_goals/annex_country.dds"
	
	selectable_in_lens = {
		always = no
	}	
	
	possible = {
		
	}

	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}

	on_war_begins = {}

	on_war_end = {}
}

dp_secession = {
	requires_interest_marker = no
	initiator_can_add_war_goals = no
	target_can_add_war_goals = no
	backing_down_always_ends_play = no
	add_infamy_for_starting_initiator_wargoals = no
	
	war_goal = secession
	
	texture = "gfx/interface/icons/war_goals/secession.dds"
	
	selectable_in_lens = {
		always = no
	}		
	
	possible = {
		scope:initiator = { is_secessionist = yes }
	}

	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			has_interventionist_fp = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = secession_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:secession_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	on_weekly_pulse = {}

	on_war_begins = {}

	on_war_end = {}
}

dp_contain_threat = {
	requires_interest_marker = no
	initiator_can_add_war_goals = no
	target_can_add_war_goals = yes
	
	war_goal = contain_threat
	
	texture = "gfx/interface/icons/war_goals/contain_threat.dds"
	
	possible = {
	
		aggressive_diplomatic_plays_permitted = yes
		
		NOT = { is_country_type = decentralized }
		NOT = { is_subject_of = scope:target_country }
		scope:target_country = {
			NOT = { is_country_type = decentralized }
			NOT = { is_subject_of = root }
		}	
		
		#custom_tooltip = {
		#	text = DIPLO_PLAY_GREAT_POWER_TRIGGER
		#	country_rank >= rank_value:great_power
		#}
		
		#THIS does not have good relations with TARGET
		custom_tooltip = {
			text = DIPLO_ACTION_RELATIONS_CORDIAL_OR_GREATER_NOT
			scope:target_country.relations:root < relations_threshold:cordial
		}
		
		#TARGET has high infamy
		custom_tooltip = {
			text = DIPLO_PLAY_OUTRAGED_TRIGGER
			"scope:target_country.infamy" >= infamy_threshold:pariah
		}
		
		#THIS does not have restricted war rights
		NOT = { has_law_or_variant = law_type:law_no_war }
		
		#THIS has some army
		NOT = { has_law_or_variant = law_type:law_peasant_levies }
		
		#No non-aggression_pact with TARGET
		has_no_non_aggression_pact_with_target = yes
		
	}

	additional_involvement_trigger = {
	
		aggressive_diplomatic_plays_permitted = yes
		
		NOT = { is_country_type = decentralized }
		NOT = { is_subject_of = scope:target_country }
		scope:target_country = {
			NOT = { is_country_type = decentralized }
			NOT = { is_subject_of = root }
		}	
		
		#custom_tooltip = {
		#	text = DIPLO_PLAY_GREAT_POWER_TRIGGER
		#	country_rank >= rank_value:great_power
		#}
		
		#THIS does not have good relations with TARGET
		custom_tooltip = {
			text = DIPLO_ACTION_RELATIONS_CORDIAL_OR_GREATER_NOT
			scope:target_country.relations:root < relations_threshold:cordial
		}
		
		#TARGET has high infamy
		custom_tooltip = {
			text = DIPLO_PLAY_OUTRAGED_TRIGGER
			"scope:target_country.infamy" >= infamy_threshold:pariah
		}
		
		#THIS does not have restricted war rights
		NOT = { has_law_or_variant = law_type:law_no_war }
		
		#THIS has some army
		NOT = { has_law_or_variant = law_type:law_peasant_levies }
		
		#No non-aggression_pact with TARGET
		has_no_non_aggression_pact_with_target = yes
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}

	on_war_begins = {}

	on_war_end = {}
	
}

dp_un_peacekeeping = {
	war_goal = annex_country
	requires_interest_marker = no
	initiator_can_add_war_goals = no
	target_can_add_war_goals = no
	add_infamy_for_starting_initiator_wargoals = no
	
	texture = "gfx/interface/icons/war_goals/contain_threat.dds"
	
	selectable_in_lens = {
		always = no
	}
	
	possible = {}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
	
}

dp_disarm_military = {
	war_goal = disarm_military
	
	texture = "gfx/interface/icons/lens_toolbar_icons/enforce_military_access break.dds"
	
	possible = {
	
		#TARGET has military
		scope:target_country = {
			NOT = { has_law_or_variant = law_type:law_peasant_levies }
		}
		
		aggressive_diplomatic_plays_permitted = yes
		
		#THIS is not decentralized
		NOT = { is_country_type = decentralized }
		scope:target_country = { NOT = { is_country_type = decentralized } }
		
		#THIS and TARGET are not revolutionary
		is_revolutionary = no is_secessionist = no
		scope:target_country = { is_revolutionary = no is_secessionist = no } 
		
		#THIS is not a subject
		is_sufficiently_independent = yes
		scope:target_country = { is_sufficiently_independent = yes }
		NOT = { is_subject_of = scope:target_country }
		scope:target_country = { NOT = { is_subject_of = root } }
		
		#Standard checks
		diplo_play_war_check_cwe = yes
		
	}

	additional_involvement_trigger = {
	
		aggressive_diplomatic_plays_permitted = yes
		
		#THIS is not decentralized
		NOT = { is_country_type = decentralized }
		scope:target_country = { NOT = { is_country_type = decentralized } }
		
		#THIS and TARGET are not revolutionary
		is_revolutionary = no is_secessionist = no
		scope:target_country = { is_revolutionary = no is_secessionist = no } 
		
		#THIS is not a subject
		is_sufficiently_independent = yes
		scope:target_country = { is_sufficiently_independent = yes }
		NOT = { is_subject_of = scope:target_country }
		scope:target_country = { NOT = { is_subject_of = root } }
		
		#Standard checks
		diplo_play_war_check_cwe = yes
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}

	on_war_begins = {}

	on_war_end = {}
	
}

dp_remove_wmd = {
	war_goal = remove_wmd
	
	texture = "gfx/interface/icons/lens_toolbar_icons/demand_nuclear_weapons_removal.dds"
	
	possible = {
	
		#TARGET has WMDs
		scope:target_country = {
			country_has_wmd = yes
		}
		
		aggressive_diplomatic_plays_permitted = yes
		
		#THIS is not decentralized
		NOT = { is_country_type = decentralized }
		scope:target_country = { NOT = { is_country_type = decentralized } }
		
		#THIS and TARGET are not revolutionary
		is_revolutionary = no is_secessionist = no
		scope:target_country = { is_revolutionary = no is_secessionist = no } 
		
		#THIS is not a subject
		is_sufficiently_independent = yes
		scope:target_country = { is_sufficiently_independent = yes }
		NOT = { is_subject_of = scope:target_country }
		scope:target_country = { NOT = { is_subject_of = root } }
		
		#Standard checks
		diplo_play_war_check_cwe = yes
		
	}

	additional_involvement_trigger = {
	
		aggressive_diplomatic_plays_permitted = yes
		
		#THIS is not decentralized
		NOT = { is_country_type = decentralized }
		scope:target_country = { NOT = { is_country_type = decentralized } }
		
		#THIS and TARGET are not revolutionary
		is_revolutionary = no is_secessionist = no
		scope:target_country = { is_revolutionary = no is_secessionist = no } 
		
		#THIS is not a subject
		is_sufficiently_independent = yes
		scope:target_country = { is_sufficiently_independent = yes }
		NOT = { is_subject_of = scope:target_country }
		scope:target_country = { NOT = { is_subject_of = root } }
		
		#Standard checks
		diplo_play_war_check_cwe = yes
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}

	on_war_begins = {}

	on_war_end = {}
	
}

dp_join_power_bloc = {
	war_goal = join_power_bloc
	texture = "gfx/interface/icons/war_goals/join_power_bloc.dds"

	possible = {
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		is_power_bloc_leader = yes # Also checked by wargoal, but here for performance reasons
		custom_tooltip = {
			text = dp_join_power_bloc_must_have_declined_invite
			any_diplomatic_catalyst = {
				is_diplomatic_catalyst_type = catalyst_declined_bloc_invite
				target = scope:target_country
			}			
		}
		diplo_play_war_check_cwe = yes
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}

	ai_acceptance_max = 100

	on_weekly_pulse = {}

	on_war_begins = {}

	on_war_end = {}
}

dp_force_nationalization = {
	requires_interest_marker = no

	war_goal = force_nationalization
	texture = "gfx/interface/icons/war_goals/force_nationalization.dds"

	possible = {
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		diplo_play_war_check_cwe = yes
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}

	selectable_in_lens = {
		
	}

	ai_acceptance_max = 100

	on_weekly_pulse = {}

	on_war_begins = {}

	on_war_end = {}
}

dp_give_foreign_investment_rights = {
	war_goal = foreign_investment_rights

	requires_interest_marker = yes

	texture = "gfx/interface/icons/war_goals/foreign_investment_rights.dds"

	add_infamy_for_starting_target_wargoals = yes

	possible = {
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		diplo_play_war_check_cwe = yes
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}

	selectable_in_lens = {
		
	}

	ai_acceptance_max = 100

	on_weekly_pulse = {}

	on_war_begins = {}

	on_war_end = {}
}

dp_native_uprising = {
	war_goal = colonization_rights
	target_can_add_war_goals = no
	
	requires_interest_marker = no
	add_infamy_for_starting_initiator_wargoals = no
	
	texture = "gfx/interface/icons/war_goals/annex_country.dds"
	
	selectable_in_lens = {
		always = no
	}
	
	possible = {
		is_country_type = decentralized
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_assisted_unification = {
	war_goal = assisted_unification
	
	texture = "gfx/interface/icons/lens_toolbar_icons/request_unification.dds"
	
	#selectable_in_lens = {
	#	always = no
	#}
	
	possible = {
		
		aggressive_diplomatic_plays_permitted = yes
		
		#THIS is not decentralized
		NOT = { is_country_type = decentralized }
		scope:target_country = { NOT = { is_country_type = decentralized } }
		
		#THIS and TARGET are not revolutionary
		is_revolutionary = no is_secessionist = no
		scope:target_country = { is_revolutionary = no is_secessionist = no } 
		
		#THIS is not a subject
		is_sufficiently_independent = yes
		scope:target_country = { is_sufficiently_independent = yes }
		NOT = { is_subject_of = scope:target_country }
		scope:target_country = { NOT = { is_subject_of = root } }
		
		#Standard checks
		diplo_play_war_check_cwe = yes
		
		#Is capable of unification
		any_country = {
			NOT = { THIS = ROOT }
			country_rank > scope:target_country.country_rank
			capital ?= { is_homeland_of_country_cultures = scope:target_country }
			aggressive_diplomatic_plays_permitted = yes
			NOR = { 
				is_diplomatic_play_enemy_of = ROOT 
				is_diplomatic_play_ally_of = scope:target_country
				is_subject_of = scope:target_country
			}
			any_primary_culture = {
				OR = { 
					is_primary_culture_of = scope:target_country
					shares_non_heritage_trait_with_any_primary_culture = scope:target_country
				}
			}
		}
		scope:target_country.capital ?= { NOT = { is_homeland_of_country_cultures = ROOT } }
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_annex_war = {
	war_goal = annex_country
	
	texture = "gfx/interface/icons/war_goals/annex_country.dds"
	
	#selectable_in_lens = {
	#	always = no
	#}
	
	possible = {
	
		#THIS is not decentralized
		NOT = { is_country_type = decentralized }
		
		#THIS and Target are not revolutionary
		is_revolutionary = no is_secessionist = no
		scope:target_country = { is_revolutionary = no is_secessionist = no }
		
		#THIS has a higher rank than TARGET
		OR = { 
			country_rank > scope:target_country.country_rank
			scope:target_country = { 
				any_scope_state = { 
					is_homeland_of_country_cultures = ROOT
					is_homeland_of_country_cultures = scope:target_country 
				} 
			}
		}
		
		#THIS does not have a subject relationship with TARGET
		NOT = { is_subject_of = scope:target_country }
		scope:target_country = { NOT = { is_subject_of = root } }
		
		#THIS can launch aggressive wars
		aggressive_diplomatic_plays_permitted = yes
		
		#Bad relations with TARGET
		custom_tooltip = {
			text = DIPLO_ACTION_RELATIONS_POOR_OR_LESS
			scope:target_country.relations:root <= relations_threshold:poor
		}
		
		#THIS does not have restricted war rights
		NOR = { 
			has_law_or_variant = law_type:law_no_war
			has_law_or_variant = law_type:law_defensive_war
		}
		
		#THIS has some army
		NOT = { has_law_or_variant = law_type:law_peasant_levies }
		
		#Ruler is jingoistic OR is a dictatorship and has a war mongering IG OR have ideological differences
		OR = { 
		
			AND = { 
				exists = ruler
				ruler ?= {
					OR = {
						has_ideology = ideology:ideology_autocratic
						has_ideology = ideology:ideology_power_hungry
					}
				}
			}
			
			AND = {
				any_interest_group = {
					is_in_government = yes
					OR = { 
						is_powerful = yes
						has_modifier = cabinet_minister_ig
					}
					leader ?= { 
						OR = {
							has_ideology = ideology:ideology_autocratic
							has_ideology = ideology:ideology_power_hungry
						}
					}
				}
			}
			
			AND = {
				has_law_or_variant = law_type:law_HOS_monarch
				scope:target_country ?= { NOT = { has_law_or_variant = law_type:law_HOS_monarch } }
			}
			
			AND = {
				has_law_or_variant = law_type:law_HOS_president
				scope:target_country ?= { NOT = { has_law_or_variant = law_type:law_HOS_president } }
			}
			
			AND = {
				has_law_or_variant = law_type:law_HOS_religious_leader
				scope:target_country ?= { NOT = { has_law_or_variant = law_type:law_HOS_religious_leader } }
			}
			
			AND = {
				has_law_or_variant = law_type:law_HOS_nationalist_leader
				scope:target_country ?= { NOT = { has_law_or_variant = law_type:law_HOS_nationalist_leader } }
			}
			
			AND = {
				has_law_or_variant = law_type:law_HOS_communist_leader
				scope:target_country ?= { NOT = { has_law_or_variant = law_type:law_HOS_communist_leader } }
			}
		
		}
		
		#No non-aggression_pact with TARGET
		has_no_non_aggression_pact_with_target = yes
		
		#Parliament and Cabinet approves
		government_has_legislative_simple_majority = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_annex_war_historical = {
	war_goal = annex_country
	initiator_can_add_war_goals = no
	target_can_add_war_goals = no
	
	texture = "gfx/interface/icons/war_goals/annex_country.dds"
	
	selectable_in_lens = {
		always = no
	}
	
	possible = {}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_humiliation = {
	war_goal = humiliation
	
	texture = "gfx/interface/icons/war_goals/humiliation.dds"
	
	possible = {	
		aggressive_diplomatic_plays_permitted = yes
		
		diplo_play_war_check_cwe = yes
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country = { is_subject_of = ROOT }
			scope:initiator = { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

### Unification Plays ###

dp_unify_germany = {
	war_goal = unification
	
	requires_interest_marker = no
	blocked_by_diplomatic_status = no
	add_infamy_for_starting_initiator_wargoals = no
	
	texture = "gfx/interface/icons/war_goals/unify_germany.dds"
	
	selectable_in_lens = {
		always = no
	}
	
	possible = {
	
		NOT = { exists = c:GER }
		
		NOT = { is_country_type = decentralized }
		
		can_form_nation = GER
		scope:target_country = { can_form_nation = GER }
		
		diplo_play_war_check_cwe = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country ?= { is_subject_of = ROOT }
			scope:initiator ?= { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_unify_italy = {
	war_goal = unification
	
	requires_interest_marker = no
	blocked_by_diplomatic_status = no
	add_infamy_for_starting_initiator_wargoals = no
	
	texture = "gfx/interface/icons/war_goals/unify_italy.dds"
	
	selectable_in_lens = {
		always = no
	}
	
	possible = {
	
		NOT = { exists = c:ITA }
		
		NOT = { is_country_type = decentralized }
		
		can_form_nation = ITA
		scope:target_country = { can_form_nation = ITA }
		
		diplo_play_war_check_cwe = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country ?= { is_subject_of = ROOT }
			scope:initiator ?= { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_unify_scandinavia = {
	war_goal = unification
	
	requires_interest_marker = no
	blocked_by_diplomatic_status = no
	add_infamy_for_starting_initiator_wargoals = no
	
	texture = "gfx/interface/icons/war_goals/unification.dds"
	
	selectable_in_lens = {
		always = no
	}
	
	possible = {
	
		NOT = { exists = c:SCA }
		
		NOT = { is_country_type = decentralized }
		
		can_form_nation = SCA
		scope:target_country = { can_form_nation = SCA }
		
		diplo_play_war_check_cwe = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country ?= { is_subject_of = ROOT }
			scope:initiator ?= { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_unify_ARF = {
	war_goal = unification
	
	requires_interest_marker = no
	blocked_by_diplomatic_status = no
	add_infamy_for_starting_initiator_wargoals = no
	
	texture = "gfx/interface/icons/war_goals/unification.dds"
	
	selectable_in_lens = {
		always = no
	}
	
	possible = {
		
		NOT = { exists = c:ARF }
		
		NOT = { is_country_type = decentralized }
		
		can_form_nation = ARF
		scope:target_country = { can_form_nation = ARF }
		
		diplo_play_war_check_cwe = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country ?= { is_subject_of = ROOT }
			scope:initiator ?= { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_unify_UAR = {
	war_goal = unification
	
	requires_interest_marker = no
	blocked_by_diplomatic_status = no
	add_infamy_for_starting_initiator_wargoals = no
	
	texture = "gfx/interface/icons/war_goals/unification.dds"
	
	selectable_in_lens = {
		always = no
	}
	
	possible = {
		
		NOT = { exists = c:UAR }
		
		NOT = { is_country_type = decentralized }
		
		can_form_nation = UAR
		scope:target_country = { can_form_nation = UAR }
		
		diplo_play_war_check_cwe = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country ?= { is_subject_of = ROOT }
			scope:initiator ?= { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_unify_benelux = {
	war_goal = unification
	
	requires_interest_marker = no
	blocked_by_diplomatic_status = no
	add_infamy_for_starting_initiator_wargoals = no
	
	texture = "gfx/interface/icons/war_goals/unification.dds"
	
	selectable_in_lens = {
		always = no
	}
	
	possible = {
		
		NOT = { exists = c:BNL }
		
		NOT = { is_country_type = decentralized }
		
		can_form_nation = BNL
		scope:target_country = { can_form_nation = BNL }
		
		diplo_play_war_check_cwe = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country ?= { is_subject_of = ROOT }
			scope:initiator ?= { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_unify_ethiopia = {
	war_goal = annex_country
	
	texture = "gfx/interface/icons/war_goals/annex_country.dds"
	
	selectable_in_lens = {
		can_form_nation = ETH
	}
	
	possible = {
	
		NOT = { exists = c:ETH }
		
		NOT = { is_country_type = decentralized }
		
		can_form_nation = ETH
		scope:target_country = { can_form_nation = ETH }
		
		diplo_play_war_check_cwe = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country ?= { is_subject_of = ROOT }
			scope:initiator ?= { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_unify_china = {
	war_goal = annex_country
	
	requires_interest_marker = no
	blocked_by_diplomatic_status = no
	add_infamy_for_starting_initiator_wargoals = no
	
	texture = "gfx/interface/icons/war_goals/annex_country.dds"
	
	selectable_in_lens = {
		can_form_nation = CHI
	}
	
	possible = {
	
		NOT = { exists = c:CHI }
		
		NOT = { is_country_type = decentralized }
		
		can_form_nation = CHI
		scope:target_country = { can_form_nation = CHI }
		
		diplo_play_war_check_cwe = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country ?= { is_subject_of = ROOT }
			scope:initiator ?= { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_unify_korea = {
	war_goal = annex_country
	
	requires_interest_marker = no
	blocked_by_diplomatic_status = no
	add_infamy_for_starting_initiator_wargoals = no
	
	texture = "gfx/interface/icons/war_goals/annex_country.dds"
	
	selectable_in_lens = {
		can_form_nation = KOR
	}
	
	possible = {
	
		NOT = { exists = c:KOR }
		
		NOT = { is_country_type = decentralized }
		
		can_form_nation = KOR
		scope:target_country = { can_form_nation = KOR }
		
		diplo_play_war_check_cwe = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country ?= { is_subject_of = ROOT }
			scope:initiator ?= { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_unify_india = {
	war_goal = annex_country
	
	texture = "gfx/interface/icons/war_goals/annex_country.dds"
	
	selectable_in_lens = {
		can_form_nation = BHT
	}
	
	possible = {
	
		NOT = { exists = c:BHT }
		
		NOT = { is_country_type = decentralized }
		
		can_form_nation = BHT
		scope:target_country = { can_form_nation = BHT }
		
		diplo_play_war_check_cwe = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country ?= { is_subject_of = ROOT }
			scope:initiator ?= { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_unify_indonesia = {
	war_goal = annex_country
	
	texture = "gfx/interface/icons/war_goals/annex_country.dds"
	
	selectable_in_lens = {
		can_form_nation = IDO
	}
	
	possible = {
	
		NOT = { exists = c:IDO }
		
		NOT = { is_country_type = decentralized }
		
		can_form_nation = IDO
		scope:target_country = { can_form_nation = IDO }
		
		diplo_play_war_check_cwe = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country ?= { is_subject_of = ROOT }
			scope:initiator ?= { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_unify_UNO = {
	war_goal = annex_country
	
	texture = "gfx/interface/icons/war_goals/annex_country.dds"
	
	selectable_in_lens = {
		can_form_nation = UNO
	}
	
	possible = {
	
		NOT = { exists = c:UNO }
		
		NOT = { is_country_type = decentralized }
		
		can_form_nation = UNO
		scope:target_country = { can_form_nation = UNO }
		
		diplo_play_war_check_cwe = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country ?= { is_subject_of = ROOT }
			scope:initiator ?= { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_leadership_germany = {
	war_goal = unification_leadership
	
	requires_interest_marker = no
	mirror_war_goal = yes
	
	texture = "gfx/interface/icons/war_goals/unify_germany.dds"
	
	selectable_in_lens = {
		always = no
	}	
	
	possible = {
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country ?= { is_subject_of = ROOT }
			scope:initiator ?= { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_leadership_italy = {
	war_goal = unification_leadership
	
	requires_interest_marker = no
	mirror_war_goal = yes
	
	texture = "gfx/interface/icons/war_goals/unify_italy.dds"
	
	selectable_in_lens = {
		always = no
	}	
	
	possible = {
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country ?= { is_subject_of = ROOT }
			scope:initiator ?= { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_leadership_scandinavia = {
	war_goal = unification_leadership
	
	requires_interest_marker = no
	mirror_war_goal = yes
	
	texture = "gfx/interface/icons/war_goals/unification.dds"
	
	selectable_in_lens = {
		always = no
	}	
	
	possible = {
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country ?= { is_subject_of = ROOT }
			scope:initiator ?= { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_leadership_ARF = {
	war_goal = unification_leadership
	
	requires_interest_marker = no
	mirror_war_goal = yes
	
	texture = "gfx/interface/icons/war_goals/unification.dds"
	
	selectable_in_lens = {
		always = no
	}	
	
	possible = {
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country ?= { is_subject_of = ROOT }
			scope:initiator ?= { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_leadership_UAR = {
	war_goal = unification_leadership
	
	requires_interest_marker = no
	mirror_war_goal = yes
	
	texture = "gfx/interface/icons/war_goals/unification.dds"
	
	selectable_in_lens = {
		always = no
	}	
	
	possible = {
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		NOT = { is_subject_of = scope:target_country }
		scope:target_country = {
			NOT = { is_country_type = decentralized }
			NOT = { is_subject_of = root }
		}	

		diplo_play_war_check_cwe = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country ?= { is_subject_of = ROOT }
			scope:initiator ?= { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_leadership_benelux = {
	war_goal = unification_leadership
	
	requires_interest_marker = no
	mirror_war_goal = yes
	
	texture = "gfx/interface/icons/war_goals/unification.dds"
	
	selectable_in_lens = {
		always = no
	}	
	
	possible = {
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country ?= { is_subject_of = ROOT }
			scope:initiator ?= { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_leadership_UNO = {
	war_goal = unification_leadership
	
	requires_interest_marker = no
	mirror_war_goal = yes
	
	texture = "gfx/interface/icons/war_goals/unification.dds"
	
	selectable_in_lens = {
		always = no
	}	
	
	possible = {
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		NOT = { is_subject_of = scope:target_country }
		scope:target_country = {
			NOT = { is_country_type = decentralized }
			NOT = { is_subject_of = root }
		}	

		diplo_play_war_check_cwe = yes
		
	}
	
	additional_involvement_trigger = {
		
		#Potential participant is willing to get involved in other's affairs
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		OR = { 
			diplo_play_additional_involvement_check_cwe = yes
			scope:target_country ?= { is_subject_of = ROOT }
			scope:initiator ?= { is_subject_of = ROOT }
		}
	
		#Has an interest in the area
		scope:initiator = {
			capital = {
				region = {
					save_temporary_scope_as = initiator_capital_region
				}
			}
		}
		OR = {
			has_interest_marker_in_region = scope:initiator_capital_region
			is_home_country_for = scope:initiator
		}
		
	}
	
	ai_acceptance_max = 100 on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}