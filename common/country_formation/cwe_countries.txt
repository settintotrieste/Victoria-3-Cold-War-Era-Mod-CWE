### This is for country tags that do not have any split tags (eg. KOR has NKO and SKO and so an entry for KOR should not be placed here; GBR has SCO, WLS, ENG and NIR and so an entry for GBR should not be placed here)

### Africa ###

#Egypt
EGY = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

#Algeria
ALG = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

ETH = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

TIG = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

ORO = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

DJI = { #Djibouti
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

MAD = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

LIB = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

MOR = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

WSA = { # Western Sahara
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

TUN = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

TRI = {
	states = { STATE_TRIPOLI }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

FEZ = {
	states = { STATE_LIBYAN_DESERT }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

CYR = {
	states = { STATE_LIBYA }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Chad
CHA = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Mauritania
MAU = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Niger
NIG = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Democratic Republic of Congo
CNG = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Katanga
KAT = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# South Kasai
SKA = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Republic of the Congo
KNG = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Rwanda
RWA = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Burundi
BRD = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Gabon
GAB = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Namibia
NMB = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Angola
ANG = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Senegal
SEN = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Guinea
GUI = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Nigeria
NGE = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

#Biafra
BIA = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

#Yoruba
YOR = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

BEN = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Togo
TOG = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Burkina Faso
BUF = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Guinea-Bissau
GNB = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# The Gambia
GAM = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Cameroon
CMR = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Ambazonia
AMB = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Somalia
SOM = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

#Somaliland
SML = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Ghana
GHA = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Ivory Coast
IVC = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Central African Republic
CAR = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# São Tomé and Príncipe
STP = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Eritrea
ERI = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { OR = { owns_entire_state_region = STATE_ERITREA is_a_decolonisation_utility_tag = yes } }
}

#Sudan
SUD = { 
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# South Sudan
SSD = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

#Darfur
DAR = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Kenya
KEN = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Uganda
UGA = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Tanganyika
TGY = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

#Zanzibar
ZAN = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Zambia
ZAM = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Malawi
MLW = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Mozambique
MOZ = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Eswatini (formerly Swaziland)
ESW = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Lesotho
LES = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Equatorial Guinea
EQG = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Cape Verde
CPV = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

#South Africa Normal
RSA = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { NOT = { exists = c:SAF } is_a_decolonisation_utility_tag = yes }
}

#South Africa Aparthied
SAF = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

#Botswana
BOT = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

#Mali
MAL = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Azawad
AZA = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

#Zimbabwe
ZIM = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

#Southern Rhodesia
RHO = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

#Sierra Leone
SIL = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Seychelles
SEY = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Mauritius
MRT = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Comoros
COM = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Reunion
RUN = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

### Middle East ###

TUR = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

UAE = {
	use_culture_states = yes #provinces = { x4D3B30 xD12977 x723F1D xFF7473 x28D126 xC9472E xE8E0A4 xA9240B x65A581 x70D020 x8C6854 x1830A7 x631F32 x69B568 xF0D021 }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

IRQ = {
	use_culture_states = yes
	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

OMA = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

PER = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

KHU = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

SYE = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

NYE = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

BHR = {
	use_culture_states = yes #provinces = { x505AE0 }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Palestine
PAL = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

ISR = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

SYR = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

JOR = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

LEB = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

#Kurdistan
KUR = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Qatar
QAT = {
	use_culture_states = yes #provinces = { x2768F8 xEF5020 }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Kuwait
KUW = {
	use_culture_states = yes #provinces = { x00F060 x68C89A xDABB9C }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Saudi Arabia
NEJ = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

### Caribbean ###

#SPM = { #Saint Pierre and Miquelon
#	use_culture_states = yes
#	
#	required_states_fraction = 0.001
#
#	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
#}

BEU = { #Bermuda
	states = { STATE_BERMUDA }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

BAH = {
	states = { STATE_BAHAMAS }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

CUB = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

HAI = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

DOM = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

PUR = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

JAM = {
	states = { STATE_JAMAICA }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# West Indies Federation
WIF = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Netherlands Antillies
DAN = {
	use_culture_states = yes #provinces = { xB03080 }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

### Europe ###

POR = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

SPA = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

CAT = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

BSQ = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

ADL = { #Andulasia
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

FRA = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

OCC = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

COR = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

BRI = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

ITA = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

MLT = { # Malta
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

SWI = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

BEL = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

FLA = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

WLL = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

NET = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

LUX = {
	use_culture_states = yes #provinces = { x5180A0 }
	
	required_states_fraction = 0.001
	
	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

ENG = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

WLS = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

SCO = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

NIR = {
	states = { STATE_ULSTER }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

SIR = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

ICL = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

DEN = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

FAR = {
	use_culture_states = yes #provinces = { x8B7790 }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

GRN = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

SRB = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

EAU = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { NOT = { exists = c:EAU } is_a_decolonisation_utility_tag = yes }
}

BOH = { #Czechia
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

SLV = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

HUN = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

SLO = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

CRO = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

BOS = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

MON = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

SER = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

KOS = { # Kosovo
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

MCD = { # North Macedonia
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

ALB = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

GRE = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

NOR = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

SWE = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

SMI = { # Sapmi
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

POL = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

ROM = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

BUL = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

FIN = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

RUS = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

EST = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

LIT = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

LAT = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

BYE = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

UKR = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

MOL = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

CYP = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { OR = { is_a_decolonisation_utility_tag = yes has_law = law_type:law_multicultural } }
}

CRI = {
	states = { STATE_CRIMEA }

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

TRE = {
	states = { STATE_ISTRIA }

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

### North America ###

CAN = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

NFL = {
	states = { STATE_NEWFOUNDLAND }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

QUE = {
	states = { STATE_QUEBEC }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

CSC = { # Cascadia
	states = { STATE_BRITISH_COLUMBIA }
	
	required_states_fraction = 0.001
	
	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

LSK = {
	states = { STATE_ALASKA }
	
	required_states_fraction = 0.001
	
	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

USA = {
	use_culture_states = yes
	
	required_states_fraction = 0.001 #required_num_states = 13

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

TEX = {
	states = { STATE_TEXAS }
	
	required_states_fraction = 0.001
	
	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

MEX = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

ZAP = { # Zapatista
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

GUA = {
	states = { STATE_GUATEMALA }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

BLZ = {
	use_culture_states = yes #provinces = { xD0B040 x02073E x7D333F }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

HON = {
	states = { STATE_HONDURAS }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

ELS = {
	states = { STATE_SAN_SALVADOR }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

NIC = {
	states = { STATE_NICARAGUA }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

COS = {
	states = { STATE_COSTA_RICA }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

PNM = {
	states = { STATE_PANAMA }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

### South East Asia ###

NVM = {
	states = { STATE_TONKIN }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

SVM = {
	states = { STATE_MEKONG }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

LAO = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

CAM = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

THI = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

BUR = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

KRN = { # Karen
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

KHN = { # Kachin
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Malaya
MLY = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Singapore
SGP = {
	use_culture_states = yes #provinces = { xC00130 }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Sabah
SAB = {
	use_culture_states = yes #provinces = { x763646 x965EF0 xC531AE xB689E6 xD0AC5F x821B67 x75D5D2 x808070 x461E0E xC0B7FD x3CCBAA xCED863 x2EE734 xD78953 }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

BRU = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

#Sarawak
SWK = {
	use_culture_states = yes #provinces = { xE9FB27 x91B40F x0141B0 x278E1E x6D3CF9 xF61121 x905338 x242519 x26DE63 xEF6FBE xFC8025 x9E0616 x1C6623 x8041B0 xC65120 x0F01DC x762FBA xE66512 x61FE7A xFB18B8 xDD9CFE xF2BC79 x6FEE71 x002310 }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

PHI = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

#Mindano
MGD = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

#Indonesia
SAK = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

ACE = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

JAV = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

BAL = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# East Timor
ETM = {
	use_culture_states = yes #provinces = { x4EE71E x3CCE91 x7F4592 }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# South Maluku
SMK = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# West Papua
WPA = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Papua New Guinea
PNG = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Solomon Islands
SOL = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Vanuatu
VAN = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Nauru
NAU = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Palau
PAU = {
	use_culture_states = yes

	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes any_primary_culture = { cu:palauan = this } }
}

# Micronesia
MIC = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes any_primary_culture = { cu:micronesian = this } }
}

# Marshall Islands
MAR = {
	use_culture_states = yes #provinces = { xF5DBBF x808011 xCD1EA0 x6e4206 }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes any_primary_culture = { cu:marshallese = this } }
}

# Tuvalu
TVU = {
	use_culture_states = yes #provinces = { x808090 }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes any_primary_culture = { cu:tuvaluan = this } }
}

# New Caledonia
NCD = {
	states = { STATE_KANAK }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Fiji
FIJ = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Wallis and Futuna
#WAF = {
#	use_culture_states = yes
#	
#	required_states_fraction = 0.001
#
#	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
#}

# Kiribati
KIR = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes any_primary_culture = { cu:kiribati = this } }
}

# Samoa
SAM = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes any_primary_culture = { cu:samoan = this } }
}

# Tonga
TON = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes any_primary_culture = { cu:tongan = this } }
}

# Niue
#NIU = {
#	use_culture_states = yes
#	
#	required_states_fraction = 0.001
#
#	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
#}

# Cook Islands
COI = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes any_primary_culture = { cu:cook_islander = this } }
}

# Polynesia
PYI = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes any_primary_culture = { cu:polynesian = this } }
}

AST = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

NZL = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

HAW = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

BOU = { # Bougainville
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

### Asia ###

TAI = { # Taiwan
	states = { STATE_FORMOSA }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

HKG = { # Hong Kong
	use_culture_states = yes #provinces = { xB03161 }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

MCA = { # Macau
	use_culture_states = yes #provinces = { xB031E0 }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

### Asia ###

SRL = { # Sri Lanka
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

MLD = { # Maldives
	use_culture_states = yes #provinces = { x95AAA3 }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

SIK = {
	use_culture_states = yes #provinces = { xE0B0E0 }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

BHU = {
	use_culture_states = yes #provinces = { x388D2C xDA7F32 x665E5B x01C109 xD10C8F x4F99DC x84F73C xE5D3E0 x60B0DF }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

NEP = {
	use_culture_states = yes #provinces = { xBCD18E x29576A x4DE9D5 xC6179B x920B51 xC1C87A x812FBE xAF5B29 x25994F x11C815 x6E233E xE18F72 xAF11F0 x4874D9 x4D5FC1 xC13072 xA2C234 x605021 x833D63 x73C2C6 x23C36A xCE4DAE xA5D1EB x1C3876 xA228F8 x218FFE x515E66 x213060 x1619ED xDA8130 xC1DA32 xE51929 xC6D4A8 }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

TIB = { # Tibet
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

BNG = { # Bangladesh
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

HND = { # India
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

PAK = { # Pakistan
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

AFG = { # Afghanistan
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

### South America ###

VNZ = {
	states = {
		STATE_MIRANDA
		STATE_BOLIVAR
		STATE_ZULIA
	}
	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

CLM = {
	states = {
		STATE_ANTIOQUIA
		STATE_CUNDINAMARCA
		STATE_CAUCA
		STATE_GUAVIARE
	}
	
	required_states_fraction = 0.001 #required_num_states = 3

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

ECU = {
	states = {
		STATE_ECUADOR
		STATE_PASTAZA
	}
	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

PEU = {
	states = {
		STATE_LIMA
		STATE_CAJAMARCA
		STATE_ICA
		STATE_AREQUIPA
		STATE_TARAPACA
	}
	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

BRZ = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Guyana
GUY = {
	use_culture_states = yes #provinces = { xA22A4C xBB88B5 x218011 x1F5190 x6B802F xA52CE3 xE600F3 xA00111 x734F81 x8E31E8 x7B4B5E x63ABD4 x3A069F x24A66B x719492 x41552E x4AE8F7 x9A88B5 xB21306 xB1960A x805A8C x518580 xC0C0D0 xADE6C2 xE85635 x9FEDF7 xAA1153 x6816FF x350473 xFAE8C5 xB3512B xD0678B xD0BA2B xE7864B xA350D5 x6C6251 }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Suriname
SUR = {
	use_culture_states = yes #provinces = { xF17CCC xA08011 xDBF640 x50D012 x29D96A xE18467 xDB86CE xE8D6CB xE6BBD3 xA9777B xF6408F x063557 xA299AE x210190 x771D68 xD8362B xF1E3BF x4B427A x0753FE x19A3D9 xC2E46E x58ABBF x3EC2E7 xB6F7B4 x43E136 x102B13 xCD1A5A x822364 x27E268 }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# French Guiana
FGY = {
	use_culture_states = yes #provinces = { x80C050 x705AB9 x439BC2 x8C64ED x61BE2C xC591AC x049E29 x20DD27 xA082D3 xF23696 xF20136 x148FB5 x9F0090 }

	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

BOL = {
	states = {
		STATE_SANTA_CRUZ
		STATE_POTOSI
		STATE_LA_PAZ
		STATE_ACRE
		STATE_ANTOFAGASTA
	}
	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

CHL = {
	states = {
		STATE_SANTIAGO
		STATE_LOS_RIOS
	}
	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

ARG = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

PRG = {
	states = {
		STATE_BAJO_PARAGUAY
		STATE_ALTO_PARAGUAY
	}
	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

URU = {
	states = {
		STATE_URUGUAY
	}
	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

FLK = { #Falkland Islands
	states = { STATE_SOUTH_ATLANTIC_ISLANDS }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

### Central Asia ###

ARM = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

DAG = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

AZB = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

GEO = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

KAZ = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

TKM = { # Turkmenistan
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Uzbekistan
UZB = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Tajikistan
TAJ = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Kyrgyzstan
KYR = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# Caucasian Imamate / Chechnya
CHC = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

### Far East Asia ###

JAP = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

PRC = { #Mainland China
	states = { STATE_BEIJING }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

ROC = { #Exiled China
	states = { STATE_NANJING }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

TAI = { # Taiwan
	states = { STATE_FORMOSA }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

HKG = { # Hong Kong
	states = { STATE_SHAOZHOU }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

MCA = { # Macau
	states = { STATE_GUANGDONG }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# North Korea
NKO = {
	states = { STATE_PYONGYANG }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

# South Korea
SKO = {
	states = { STATE_SEOUL }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

MGL = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

XIN = {
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

ETS = { # East Turkestan
	use_culture_states = yes
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}

MCK = {
	states = { STATE_SOUTHERN_MANCHURIA }
	
	required_states_fraction = 0.001

	ai_will_do = { always = yes } possible = { is_a_decolonisation_utility_tag = yes }
}