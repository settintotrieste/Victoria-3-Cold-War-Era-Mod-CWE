generate_heir_for_dynastic_republic = {

	#Generate Heirs for Dynastic Republics
	if = {
		limit = { 
			is_a_dynastic_government = yes 
			NOT = { exists = heir }
		}
		if = {
			limit = { 
				ruler ?= { 
					has_variable = dynastic_ruler_enabled 
					age >= 90
				}
			}
			
			#Mark
			ruler ?= {
				interest_group = { save_scope_as = ruler_interest_group }
				culture = { save_scope_as = ruler_culture }
				religion = { save_scope_as = ruler_religion }
				last_name = { save_scope_as = ruler_last_name }
			}
			
			#Child
			create_character = {
				heir = yes
				age = { 50 65 }
				interest_group = scope:ruler_interest_group
				culture = scope:ruler_culture
				religion = scope:ruler_religion
				last_name = scope:ruler_last_name
				on_created = {
					set_variable = dynastic_ruler_enabled 
				}
			}
		}
		else_if = {
			limit = { 
				ruler ?= { 
					has_variable = dynastic_ruler_enabled 
					age >= 80
				}
			}
			
			#Mark
			ruler ?= {
				interest_group = { save_scope_as = ruler_interest_group }
				culture = { save_scope_as = ruler_culture }
				religion = { save_scope_as = ruler_religion }
				last_name = { save_scope_as = ruler_last_name }
			}
			
			#Child
			create_character = {
				heir = yes
				age = { 40 55 }
				interest_group = scope:ruler_interest_group
				culture = scope:ruler_culture
				religion = scope:ruler_religion
				last_name = scope:ruler_last_name
				on_created = {
					set_variable = dynastic_ruler_enabled 
				}
			}
		}
		else_if = {
			limit = { 
				ruler ?= { 
					has_variable = dynastic_ruler_enabled 
					age >= 70
				}
			}
			
			#Mark
			ruler ?= {
				interest_group = { save_scope_as = ruler_interest_group }
				culture = { save_scope_as = ruler_culture }
				religion = { save_scope_as = ruler_religion }
				last_name = { save_scope_as = ruler_last_name }
			}
			
			#Child
			create_character = {
				heir = yes
				age = { 30 45 }
				interest_group = scope:ruler_interest_group
				culture = scope:ruler_culture
				religion = scope:ruler_religion
				last_name = scope:ruler_last_name
				on_created = {
					set_variable = dynastic_ruler_enabled 
				}
			}
		}
		else_if = {
			limit = { 
				ruler ?= { 
					has_variable = dynastic_ruler_enabled 
					age >= 60
				}
			}
			
			#Mark
			ruler ?= {
				interest_group = { save_scope_as = ruler_interest_group }
				culture = { save_scope_as = ruler_culture }
				religion = { save_scope_as = ruler_religion }
				last_name = { save_scope_as = ruler_last_name }
			}
			
			#Child
			create_character = {
				heir = yes
				age = { 20 35 }
				interest_group = scope:ruler_interest_group
				culture = scope:ruler_culture
				religion = scope:ruler_religion
				last_name = { save_scope_as = ruler_last_name }
				on_created = {
					set_variable = dynastic_ruler_enabled 
				}
			}
		}
		else_if = {
			limit = { 
				ruler ?= { 
					has_variable = dynastic_ruler_enabled 
					age >= 50
				}
			}
			
			#Mark
			ruler ?= {
				interest_group = { save_scope_as = ruler_interest_group }
				culture = { save_scope_as = ruler_culture }
				religion = { save_scope_as = ruler_religion }
				last_name = { save_scope_as = ruler_last_name }
			}
			
			#Child
			create_character = {
				heir = yes
				age = { 21 25 }
				interest_group = scope:ruler_interest_group
				culture = scope:ruler_culture
				religion = scope:ruler_religion
				last_name = scope:ruler_last_name
				on_created = {
					set_variable = dynastic_ruler_enabled 
				}
			}
		}
		else_if = {
			limit = { 
				ruler ?= { 
					has_variable = dynastic_ruler_enabled 
					age >= 40
				}
			}
			
			#Mark
			ruler ?= {
				interest_group = { save_scope_as = ruler_interest_group }
				culture = { save_scope_as = ruler_culture }
				religion = { save_scope_as = ruler_religion }
				last_name = { save_scope_as = ruler_last_name }
			}
			
			#Sibling
			create_character = {
				heir = yes
				age = { 21 35 }
				interest_group = scope:ruler_interest_group
				culture = scope:ruler_culture
				religion = scope:ruler_religion
				last_name = scope:ruler_last_name
				on_created = {
					set_variable = dynastic_ruler_enabled 
				}
			}
		}
		else_if = {
			limit = { 
				ruler ?= { 
					has_variable = dynastic_ruler_enabled 
					age >= 30
				}
			}
			
			#Mark
			ruler ?= {
				interest_group = { save_scope_as = ruler_interest_group }
				culture = { save_scope_as = ruler_culture }
				religion = { save_scope_as = ruler_religion }
				last_name = { save_scope_as = ruler_last_name }
			}
			
			#Sibling
			create_character = {
				heir = yes
				age = { 21 30 }
				interest_group = scope:ruler_interest_group
				culture = scope:ruler_culture
				religion = scope:ruler_religion
				last_name = scope:ruler_last_name
				on_created = {
					set_variable = dynastic_ruler_enabled 
				}
			}
		}
		else_if = {
			limit = { 
				ruler ?= { 
					has_variable = dynastic_ruler_enabled 
					age < 30
				}
			}
			
			#Mark
			ruler ?= {
				interest_group = { save_scope_as = ruler_interest_group }
				culture = { save_scope_as = ruler_culture }
				religion = { save_scope_as = ruler_religion }
				last_name = { save_scope_as = ruler_last_name }
			}
			
			#Sibling
			create_character = {
				heir = yes
				age = { 21 25 }
				interest_group = scope:ruler_interest_group
				culture = scope:ruler_culture
				religion = scope:ruler_religion
				last_name = scope:ruler_last_name
				on_created = {
					set_variable = dynastic_ruler_enabled 
				}
			}
		}
	}

}