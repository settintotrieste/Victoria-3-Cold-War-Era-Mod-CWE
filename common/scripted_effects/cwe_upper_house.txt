calculate_upper_house_composition = {

	if = {
		limit = { has_law_or_variant = law_type:law_propotional_representation }
		calculate_upper_house_composition_PR = yes
	}
	else_if = {
		limit = { has_law_or_variant = law_type:law_jefferson_system }
		calculate_upper_house_composition_jefferson = yes
	}
	else = { 
		calculate_upper_house_composition_FTP = yes 
	}

}

calculate_upper_house_composition_FTP = {

	#Calculate pro-government senators
	set_variable = {
		name = pro_government_senators_var
		value = 0
	}
	
	#Add Pro-Government members
	if = { limit = { exists = ig:ig_industrialists } ig:ig_industrialists = { save_scope_as = ig_industrialists_scope_ig } }
	if = { limit = { exists = ig:ig_petty_bourgeoisie } ig:ig_petty_bourgeoisie = { save_scope_as = ig_petty_bourgeoisie_scope_ig } }
	if = { limit = { exists = ig:ig_trade_unions } ig:ig_trade_unions = { save_scope_as = ig_trade_unions_scope_ig } }
	if = { limit = { exists = ig:ig_landowners } ig:ig_landowners = { save_scope_as = ig_landowners_scope_ig } }
	if = { limit = { exists = ig:ig_devout } ig:ig_devout = { save_scope_as = ig_devout_scope_ig } }
	if = { limit = { exists = ig:ig_rural_folk } ig:ig_rural_folk = { save_scope_as = ig_rural_folk_scope_ig } }
	if = { limit = { exists = ig:ig_intelligentsia } ig:ig_intelligentsia = { save_scope_as = ig_intelligentsia_scope_ig } }
	if = { limit = { exists = ig:ig_armed_forces } ig:ig_armed_forces = { save_scope_as = ig_armed_forces_scope_ig } }
	
	#Compute seats
	calculate_UH_pro_government_senators_FTP = yes
	calculate_arrange_seats_UH = yes
	
	#Determine if a simple majority exists
	set_variable = {
		name = upper_house_majority_var
		value = root.var:pro_government_senators_var
	}
	if = {
		limit = { root.var:upper_house_majority_var > 50 }
		set_variable = upper_house_has_simple_majority
	}
	if = {
		limit = { root.var:upper_house_majority_var > 60 }
		set_variable = upper_house_has_filibuster_majority
	}
	if = {
		limit = { root.var:upper_house_majority_var > 66.7 }
		set_variable = upper_house_has_super_majority
	}
	if = {
		limit = { 
			root.var:upper_house_majority_var <= 50 
			has_variable = upper_house_has_simple_majority
		}
		remove_variable = upper_house_has_simple_majority
	}
	if = {
		limit = { 
			root.var:upper_house_majority_var <= 60 
			has_variable = upper_house_has_filibuster_majority
		}
		remove_variable = upper_house_has_filibuster_majority
	}
	if = {
		limit = { 
			root.var:upper_house_majority_var <= 66.7 
			has_variable = upper_house_has_super_majority
		}
		remove_variable = upper_house_has_super_majority
	}
	
	post_notification = upper_house_composition_updated

}

calculate_upper_house_composition_jefferson = {

	#Calculate pro-government senators
	set_variable = {
		name = pro_government_senators_var
		value = 0
	}
	
	#Add Pro-Government members
	if = { limit = { exists = ig:ig_industrialists } ig:ig_industrialists = { save_scope_as = ig_industrialists_scope_ig } }
	if = { limit = { exists = ig:ig_petty_bourgeoisie } ig:ig_petty_bourgeoisie = { save_scope_as = ig_petty_bourgeoisie_scope_ig } }
	if = { limit = { exists = ig:ig_trade_unions } ig:ig_trade_unions = { save_scope_as = ig_trade_unions_scope_ig } }
	if = { limit = { exists = ig:ig_landowners } ig:ig_landowners = { save_scope_as = ig_landowners_scope_ig } }
	if = { limit = { exists = ig:ig_devout } ig:ig_devout = { save_scope_as = ig_devout_scope_ig } }
	if = { limit = { exists = ig:ig_rural_folk } ig:ig_rural_folk = { save_scope_as = ig_rural_folk_scope_ig } }
	if = { limit = { exists = ig:ig_intelligentsia } ig:ig_intelligentsia = { save_scope_as = ig_intelligentsia_scope_ig } }
	if = { limit = { exists = ig:ig_armed_forces } ig:ig_armed_forces = { save_scope_as = ig_armed_forces_scope_ig } }
	
	#Calculate Seats
	calculate_UH_pro_government_senators_jefferson = yes
	calculate_arrange_seats_UH = yes
	
	#Determine if a simple majority exists
	set_variable = {
		name = upper_house_majority_var
		value = root.var:pro_government_senators_var
	}
	if = {
		limit = { root.var:upper_house_majority_var > 50 }
		set_variable = upper_house_has_simple_majority
	}
	if = {
		limit = { root.var:upper_house_majority_var > 60 }
		set_variable = upper_house_has_filibuster_majority
	}
	if = {
		limit = { root.var:upper_house_majority_var > 66.7 }
		set_variable = upper_house_has_super_majority
	}
	if = {
		limit = { 
			root.var:upper_house_majority_var <= 50 
			has_variable = upper_house_has_simple_majority
		}
		remove_variable = upper_house_has_simple_majority
	}
	if = {
		limit = { 
			root.var:upper_house_majority_var <= 60 
			has_variable = upper_house_has_filibuster_majority
		}
		remove_variable = upper_house_has_filibuster_majority
	}
	if = {
		limit = { 
			root.var:upper_house_majority_var <= 66.7 
			has_variable = upper_house_has_super_majority
		}
		remove_variable = upper_house_has_super_majority
	}
	
	post_notification = upper_house_composition_updated

}

calculate_upper_house_composition_PR = {

	#Calculate pro-government senators
	set_variable = {
		name = pro_government_senators_var
		value = 0
	}
	
	#Add Pro-Government members
	if = { limit = { exists = ig:ig_industrialists } ig:ig_industrialists = { save_scope_as = ig_industrialists_scope_ig } }
	if = { limit = { exists = ig:ig_petty_bourgeoisie } ig:ig_petty_bourgeoisie = { save_scope_as = ig_petty_bourgeoisie_scope_ig } }
	if = { limit = { exists = ig:ig_trade_unions } ig:ig_trade_unions = { save_scope_as = ig_trade_unions_scope_ig } }
	if = { limit = { exists = ig:ig_landowners } ig:ig_landowners = { save_scope_as = ig_landowners_scope_ig } }
	if = { limit = { exists = ig:ig_devout } ig:ig_devout = { save_scope_as = ig_devout_scope_ig } }
	if = { limit = { exists = ig:ig_rural_folk } ig:ig_rural_folk = { save_scope_as = ig_rural_folk_scope_ig } }
	if = { limit = { exists = ig:ig_intelligentsia } ig:ig_intelligentsia = { save_scope_as = ig_intelligentsia_scope_ig } }
	if = { limit = { exists = ig:ig_armed_forces } ig:ig_armed_forces = { save_scope_as = ig_armed_forces_scope_ig } }
	
	#Calculate seats
	calculate_UH_pro_government_senators_PR = yes
	calculate_arrange_seats_UH = yes
	
	#Determine if a simple majority exists
	set_variable = {
		name = upper_house_majority_var
		value = root.var:pro_government_senators_var
	}
	if = {
		limit = { root.var:upper_house_majority_var > 50 }
		set_variable = upper_house_has_simple_majority
	}
	if = {
		limit = { root.var:upper_house_majority_var > 60 }
		set_variable = upper_house_has_filibuster_majority
	}
	if = {
		limit = { root.var:upper_house_majority_var > 66.7 }
		set_variable = upper_house_has_super_majority
	}
	if = {
		limit = { 
			root.var:upper_house_majority_var <= 50 
			has_variable = upper_house_has_simple_majority
		}
		remove_variable = upper_house_has_simple_majority
	}
	if = {
		limit = { 
			root.var:upper_house_majority_var <= 60 
			has_variable = upper_house_has_filibuster_majority
		}
		remove_variable = upper_house_has_filibuster_majority
	}
	if = {
		limit = { 
			root.var:upper_house_majority_var <= 66.7 
			has_variable = upper_house_has_super_majority
		}
		remove_variable = upper_house_has_super_majority
	}
	
	post_notification = upper_house_composition_updated

}

set_as_bipartisan_partner = {
	
	set_variable = { name = bipartisan_participant }
	save_scope_as = bipartisan_participant_ig
	custom_tooltip = {
		text = bipartisan_participant_identify_label.tt
	}
	owner = { 
		set_variable = has_bipartisan_plan
		if = { 
			limit = { has_journal_entry = je_upper_house }
			calculate_upper_house_composition = yes
		}
		if = { 
			limit = { has_journal_entry = je_lower_house }
			calculate_lower_house_composition = yes
		}
	}

}