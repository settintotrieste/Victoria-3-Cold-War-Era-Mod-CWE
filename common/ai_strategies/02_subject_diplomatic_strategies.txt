ai_strategy_subject_break_free = {
	icon = "gfx/interface/icons/ai_strategy_icons/subject_break_free.dds"

	type = diplomatic

	will_form_power_bloc = {
		generic_power_bloc_will_do_conditions = yes
	}

	undesirable_infamy_level = {
		value = 25
	}

	unacceptable_infamy_level = {
		value = 50
	}

	obligation_value = {
		# Rebellious AI wants to use obligations to break free
		if = {
			limit = {
				this.overlord ?= scope:target_country
			}
			add = 50
		}
		else = {
			add = 20
		}
	}

	diplomatic_play_boldness = {
		value = 50
	}

	recklessness = {
		value = 0.5
	}

	aggression = {
		value = 0.5 # Should focus mainly on overlord

		if = {
			limit = {
				is_subject_of = scope:target_country
			}
			add = 10
		}
	}

	building_group_weights = {
		bg_munition_plants = 1.5
		bg_arms_industries = 1.5
		bg_artillery_foundries = 1.5
		bg_military_shipyards = 1.5
	}

	wargoal_weights = {
		independence = 5.0
		increase_autonomy = 0.5
	}

	secret_goal_scores = {
		antagonize = {
			trigger = {
				is_subject_of = scope:target_country
			}
			score = {
				value = 10000
			}
		}
		none = {
			trigger = {
				is_subject_of = scope:target_country
			}
			score = {
				value = -10000
			}
		}
		defy = {
			trigger = {
				is_subject_of = scope:target_country
			}
			score = {
				value = -10000
			}
		}
		comply = {
			trigger = {
				is_subject_of = scope:target_country
			}
			score = {
				value = -10000
			}
		}
		befriend = {
			trigger = {
				scope:target_country = { country_rank >= root.country_rank }
				can_be_reached_by_target_country = yes			
			}
			score = {
				if = {
					limit = {
						scope:target_country = {
							has_diplomatic_pact = {
								who = root.overlord
								type = rivalry
							}
						}
					}
					add = 500
				}
				else_if = {
					limit = {
						scope:target_country = { country_rank >= root.overlord.country_rank }
					}
					add = 100
				}
			}
		}
	}
	
	become_subject_value = {
		value = -100
	}

	diplomatic_play_support = {
		value = 0

		if = { # Enemies of our overlord
			limit = {
				scope:enemy_country = { root.overlord ?= this }
			}
			add = {
				value = 50
				desc = "AI_STRATEGY_SUPPORT_BREAK_FREE_ENEMY_OF_OVERLORD"
			}
		}
	}

	treaty_category_scores = {
		military_defense = {
			value = 5
		}
	}

	possible = {
		is_subject = yes
		liberty_desire >= 50
	}

	weight = {
		value = liberty_desire
		subtract = 50
		multiply = 2

		# AI implications for Power Blocs: 'Break Free' strategy is more likely for Blocs with Exploitation of Members
		if = {
			limit = {
				power_bloc ?= {
					has_principle_group = principle_group:principle_group_exploit_members
				}
			}
			multiply = 2.0
		}

		# AI implications for Power Blocs: 'Break Free' strategy is more likely for Blocs with Vassalization
		if = {
			limit = {
				power_bloc ?= {
					has_principle_group = principle_group:principle_group_vassalization
				}
			}
			multiply = 1.5
		}

		# Subjects should not start the game wanting independence unless we specifically script this strategy for them
		if = {
			limit = {
				game_date < 1950.1.2
			}
			multiply = 0
		}
	}
}

ai_strategy_subject_strengthen_self = {
	icon = "gfx/interface/icons/ai_strategy_icons/subject_strengthen_self.dds"

	type = diplomatic
	
	will_form_power_bloc = {
		generic_power_bloc_will_do_conditions = yes
	}

	undesirable_infamy_level = {
		value = 50
	}

	unacceptable_infamy_level = {
		value = 75
	}

	obligation_value = {
		# Rebellious AI wants to use obligations to break free
		if = {
			limit = {
				this.overlord ?= scope:target_country
			}
			add = 50
		}
		else = {
			add = 20
		}
	}

	diplomatic_play_boldness = {
		value = 25
	}

	recklessness = {
		value = 0.25
	}

	aggression = {
		value = 1

		# Not very likely to directly attack overlord
		if = {
			limit = {
				is_subject_of = scope:target_country
			}
			add = -0.75
		}

		# Opportunistic colonization is self-strengthening, right?
		if = {
			limit = {
				NOT = { is_country_type = unrecognized }
				scope:target_country = { is_country_type = unrecognized }
			}
			add = 1
		}
	}

	building_group_weights = {
		bg_munition_plants = 1.5
		bg_arms_industries = 1.5
		bg_artillery_foundries = 1.5
		bg_military_shipyards = 1.5
	}

	wargoal_weights = {
		independence = 0.0
		increase_autonomy = 5.0
		conquer_state = 1.5
		return_state = 1.5
	}

	secret_goal_scores = {
		defy = {
			trigger = {
				is_subject_of = scope:target_country
			}
			score = {
				value = 10000
			}
		}
		antagonize = {
			trigger = {
				is_subject_of = scope:target_country
			}
			score = {
				value = -10000
			}
		}
		none = {
			trigger = {
				is_subject_of = scope:target_country
			}
			score = {
				value = -10000
			}
		}
		comply = {
			trigger = {
				is_subject_of = scope:target_country
			}
			score = {
				value = -10000
			}
		}
		befriend = {
			trigger = {
				is_subject_of = scope:target_country
			}
			score = {
				value = -10000
			}
		}
		conquer = {
			trigger = {
				can_reach_target_country = { TARGET_COUNTRY = scope:target_country }
				has_strategic_adjacency = scope:target_country
				scope:target_country.country_rank <= root.country_rank			
			}
			score = {
				if = {
					limit = {
						NOT = { is_country_type = unrecognized }
						scope:target_country = { NOT = { is_country_type = unrecognized } }
					}
					add = 25
				}
				else_if = {
					limit = {
						scope:target_country = { is_country_type = unrecognized }
					}
					add = 50
				}
			}
		}
	}
	
	become_subject_value = {
		value = -50
	}

	diplomatic_play_support = {
		value = 0

		if = { # More opportunistic
			limit = {
				scope:enemy_country = { root.overlord ?= this }
			}
			add = {
				value = 20
				desc = "AI_STRATEGY_SUPPORT_STRENGTHEN_SELF_OVERLORD_DISTRACTED"
			}
		}
	}

	treaty_category_scores = {
		military_defense = {
			value = 4
		}

		economy = {
			value = 3
		}
	}

	possible = {
		is_subject = yes
		liberty_desire >= 25
	}

	weight = {
		value = liberty_desire

		# AI implications for Power Blocs: 'Increased Self-Reliance' strategy is more likely for Blocs with Militarized Industry
		if = {
			limit = {
				liberty_desire >= 50
				power_bloc ?= {
					has_principle_group = principle_group:principle_group_military_industry
				}
			}
			multiply = 1.5
		}

	}
}

ai_strategy_subject_maintain_autonomy = {
	icon = "gfx/interface/icons/ai_strategy_icons/subject_maintain_autonomy.dds"

	type = diplomatic
	
	will_form_power_bloc = {
		generic_power_bloc_will_do_conditions = yes
	}

	undesirable_infamy_level = {
		value = 25
	}

	unacceptable_infamy_level = {
		value = 50
	}

	diplomatic_play_boldness = {
		value = -10
	}

	recklessness = {
		value = -0.25
	}

	wargoal_weights = {
		independence = 0.0
		increase_autonomy = 0.5
	}

	secret_goal_scores = {
		none = {
			trigger = {
				is_subject_of = scope:target_country
			}
			score = {
				value = 10000
			}
		}
		antagonize = {
			trigger = {
				is_subject_of = scope:target_country
			}
			score = {
				value = -10000
			}
		}
		defy = {
			trigger = {
				is_subject_of = scope:target_country
			}
			score = {
				value = -10000
			}
		}
		comply = {
			trigger = {
				is_subject_of = scope:target_country
			}
			score = {
				value = -10000
			}
		}
		befriend = {
			trigger = {
				is_subject_of = scope:target_country
			}
			score = {
				value = -10000
			}
		}
		conquer = {
			trigger = {
				can_reach_target_country = { TARGET_COUNTRY = scope:target_country }
				has_strategic_adjacency = scope:target_country
				scope:target_country.country_rank <= root.country_rank			
			}
			score = {
				if = {
					limit = {
						NOT = { is_country_type = unrecognized }
						scope:target_country = { NOT = { is_country_type = unrecognized } }
					}
					add = 15
				}
				else_if = {
					limit = {
						scope:target_country = { is_country_type = unrecognized }
					}
					add = 25
				}
			}
		}
	}
	
	become_subject_value = {
		value = -25
	}

	treaty_category_scores = {
		military_defense = {
			value = 3
		}

		expansion = {
			value = -3
		}
	}

	possible = {
		is_subject = yes
	}

	weight = {
		value = 100
		subtract = liberty_desire
	}
}

ai_strategy_subject_overlord_compliance = {
	icon = "gfx/interface/icons/ai_strategy_icons/subject_overlord_compliance.dds"

	type = diplomatic
	
	will_form_power_bloc = {
		generic_power_bloc_will_do_conditions = yes
	}

	undesirable_infamy_level = {
		value = 10
	}

	unacceptable_infamy_level = {
		value = 25
	}

	diplomatic_play_boldness = {
		value = -25
	}

	recklessness = {
		value = -0.5
	}

	aggression = {
		value = 0.25
	}

	wargoal_weights = {
		independence = 0.0
		increase_autonomy = 0.0
	}

	secret_goal_scores = {
		comply = {
			trigger = {
				is_subject_of = scope:target_country
			}
			score = {
				value = 10000
			}
		}
		antagonize = {
			trigger = {
				is_subject_of = scope:target_country
			}
			score = {
				value = -10000
			}
		}
		none = {
			score = {
				if = {
					limit = {
						is_subject_of = scope:target_country
					}
					value = -10000
				}
				else = {
					value = 200
				}		
			}
		}
		defy = {
			trigger = {
				is_subject_of = scope:target_country
			}
			score = {
				value = -10000
			}
		}
	}
	
	become_subject_value = {
		value = 50
	}

	diplomatic_play_support = {
		value = 0

		if = {
			limit = {
				scope:country = { root.overlord ?= this }
			}
			add = {
				value = 25
				desc = "AI_STRATEGY_SUPPORT_COMPLIANCE_FOLLOW_OVERLORD"
			}
		}
	}

	treaty_category_scores = {
		military_defense = {
			value = 3
		}

		power_bloc = {
			value = 2
		}
	}

	possible = {
		is_subject = yes
		liberty_desire <= 50
		NOT = { is_diplomatic_play_enemy_of = overlord }
	}

	weight = {
		value = 50
		subtract = liberty_desire
		multiply = 2
	}
}