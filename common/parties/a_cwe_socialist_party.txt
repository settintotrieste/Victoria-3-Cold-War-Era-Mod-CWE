#Economic Spectrum ->  Far Left & Left
#Political Spectrum -> Right

socialist_party = {
	color = { 255 0 0 }
	
	name = {
		first_valid = {
			triggered_desc = {
				desc = party_socialist_faction
				trigger = {
					OR = { 
						has_law_or_variant = law_type:law_parties_not_allowed
						has_law_or_variant = law_type:law_single_party_state
					}
				}
			}
			triggered_desc = {
				desc = party_socialist
				trigger = {
					always = yes
				}
			}
		}
	}
	
	icon = {
		default = "/gfx/interface/icons/political_parties/social_democrats.dds"
	}
	
	# THIS IS RUN ONLY ONCE AT GAME START
	valid_for_country = {
		always = yes
	}
	
	available_for_interest_group = {
		NOT = { has_modifier = IG_expelled_socialist_party }
		IG_not_excluded_from_party_membership = yes
		OR = {
			owner = { NOR = { has_law_or_variant = law_type:law_parties_not_allowed has_law_or_variant = law_type:law_dominant_party_state } }
			is_in_government = yes ig_clout >= 0.15
		}
	}
	
	join_weight = {
		value = {
			value = -8
			desc = "base_value"
		}
		
		#IG has correct leader political leaning
		if = {
			limit = {
				exists = leader leader = {
					OR = { 
						has_ideology = ideology:ideology_elitist
						has_ideology = ideology:ideology_democratic
					}
				}
			}
			add = {
				value = 1
				desc = "from_correct_leader_political_leaning"
			}
		}
		
		#IG has correct IG general political leaning
		if = {
			limit = {
				OR = { 
					has_ideology = ideology:ideology_authoritarian
					has_ideology = ideology:ideology_communitarian
				}
			}
			add = {
				value = 2
				desc = "from_correct_ig_general_political_leaning"
			}
		}
		
		#IG has correct IG specific political leaning
		if = {
			limit = {
				OR = {
					has_ideology = ideology:ideology_progressive
					
					has_ideology = ideology:ideology_communist
					has_ideology = ideology:ideology_republican
					has_ideology = ideology:ideology_militarist
				}
			}
			add = {
				value = 3
				desc = "from_correct_ig_specific_political_leaning"
			}
		}
		
		#IG has correct IG general economic leaning
		if = {
			limit = {
				OR = {
					has_ideology = ideology:ideology_left_economy
					has_ideology = ideology:ideology_far_left_economy
				}
			}
			add = {
				value = 4
				desc = "from_correct_general_economic_leaning"
			}
		}
		
		#IG has correct IG specific economic leaning
		if = {
			limit = {
				OR = { 
					has_ideology = ideology:ideology_nationalist
					has_ideology = ideology:ideology_regionalist
				}
			}
			add = {
				value = 1
				desc = "from_correct_specific_economic_leaning"
			}
		}
		
		if = { # Powerful parties attract power hungry IGs
			limit = {
				#THIS IG has a power hungry leader
				OR = { 
					has_ideology = ideology:ideology_autocratic
					has_ideology = ideology:ideology_power_hungry
				}
				
				is_powerful = no
			
				#Centrist party has a strong IG
				exists = owner.py:socialist_party
				owner = {
					any_interest_group = {
						is_member_of_party = owner.py:socialist_party
						OR = { is_powerful = yes is_in_government = yes }
					}
				}
			}
			add = {
				value = 4
				desc = "from_powerful_party"
			}
		}
		
		#Two Party State
		if = {
			limit = {
				owner = { has_law_or_variant = law_type:law_first_past_the_post }
				owner.py:socialist_party ?= { any_member = { is_powerful = yes } }
				party ?= { NOT = { any_member = { is_powerful = yes } } }
				is_powerful = no
				is_in_government = no
			}
			add = {
				value = 10
				desc = "from_voting_system"
			}
		}
		else_if = {
			limit = {
				owner = { has_law_or_variant = law_type:law_jefferson_system }
				owner.py:socialist_party ?= { any_member = { is_powerful = yes } }
				party ?= { NOT = { any_member = { is_powerful = yes } } }
				is_powerful = no
				is_in_government = no
			}
			add = {
				value = 5
				desc = "from_voting_system"
			}
		}
		if = {
			limit = {
				owner = { has_law_or_variant = law_type:law_first_past_the_post }
				OR = { 
					owner.py:socialist_party ?= { NOT = { any_member = { is_powerful = yes } } }
					party ?= { any_member = { is_powerful = yes } }
				}
				is_powerful = no
				is_in_government = no
			}
			add = {
				value = -10
				desc = "from_voting_system"
			}
		}
		else_if = {
			limit = {
				owner = { has_law_or_variant = law_type:law_jefferson_system }
				OR = { 
					owner.py:socialist_party ?= { NOT = { any_member = { is_powerful = yes } } }
					party ?= { any_member = { is_powerful = yes } }
				}
				is_powerful = no
				is_in_government = no
			}
			add = {
				value = -5
				desc = "from_voting_system"
			}
		}
		
	}
	
}
