BUILDINGS = {
	
	c:AST = {
		every_scope_state = { limit = { state_has_sufficient_population = yes }
			if = {
				limit = { is_incorporated = yes }
				effect_starting_buildings_4 = yes
			}
			else = {
				effect_starting_buildings_1 = yes
			}
		}
		s:STATE_NAURU = {
			region_state:AST = {
				create_building = {
					building = "building_guano_island"
					level = 1
					reserves = 1
				}
			}
		}
	}
	
	c:NZL = {
		every_scope_state = { limit = { state_has_sufficient_population = yes }
			if = {
				limit = { is_incorporated = yes }
				effect_starting_buildings_4 = yes
			}
			else = {
				effect_starting_buildings_1 = yes
			}
		}
	}
	
	c:TON = {
		every_scope_state = { limit = { state_has_sufficient_population = yes }
			if = {
				limit = { is_incorporated = yes }
				effect_starting_buildings_1 = yes
			}
		}
	}
	
	c:VAN = {
		every_scope_state = { limit = { state_has_sufficient_population = yes }
			if = {
				limit = { is_incorporated = yes }
				effect_starting_buildings_1 = yes
			}
		}
	}
	
	c:HAW = {
		every_scope_state = { limit = { state_has_sufficient_population = yes }
			if = {
				limit = { is_incorporated = yes }
				effect_starting_buildings_1 = yes
			}
		}
	}

}