# Map mode properties:
# has_fog_of_war: enables/disables fog of war. Defaults to `yes`
# soundeffect: defines an audio event that will be played when the map mode becomes active
#			   and stopped when the map mode is no longer active
#			   can be defined multiple times to associate multiple events with the same map mode


### Textures ###
# Standard
@this_is_you = "this_is_you.dds"							# Player
@color_green = "ph_green.dds"								# Usually: Can interact
@color_red = "ph_red.dds"									# Usually: Cannot interact, but valid
@color_orange = "orange.dds"								# Usually: Active, cannot deactivate
@color_yellow = "ph_yellow.dds"								# Usually: Active, cannot deactivate
@color_blue = "ph_blue.dds"									# Usually: Active, cannot deactivate
@color_pink = "ph_pink.dds"									# Usually: placeholder

# Diplo
@war_this_is_you = "you_military.dds"						# Player
@war_this_is_you_warleader = "you_warleader.dds"			# Player Warleader
@war_ally = "ally_military.dds"								# Ally
@war_ally_warleader = "ally_warleader.dds"					# Ally Warleader
@war_enemy = "enemy_military.dds"							# Enemy
@war_enemy_warleader = "enemy_warleader.dds"				# Enemy Warleader

# Relations
@hostile = "hostile.dds"									# Hostile
@cold = "cold.dds"											# Cold
@poor = "poor.dds"											# Poor
@neutral = "neutral.dds"									# Neutral
@friendly = "friendly.dds"									# Friendly
@amicable = "amicable.dds"									# Amicable
@cordial = "cordial.dds"									# Cordial

# Attitude
@belligerent_domineering = "belligerent_domineering.dds"	# Belligerent | Domineering
@antagonistic_rebellious = "antagonistic_rebellious.dds"	# Antagonistic | Rebellious
@wary = "wary.dds"											# Wary
@disinterested = "disinterested.dds"						# Disinterested
@cautious = "cautious.dds"									# Cautious
@conciliatory_cooperative = "conciliatory_cooperative.dds"	# Conciliatory | Cooperative
@loyal_protective = "loyal_protective.dds"					# Loyal | Protective
@genial = "genial.dds"										# Genial

## Interactions ##
# Declare interest
@declare_interest_active = "declare_interest_blue.dds"		# Declared interest, Can Deactivate
@declare_interest_orange = "declare_interest_orange.dds"	# Cannot Activate, but is valid

# Market
@market_connection_active = "import_export.dds"				# Trade route connected

# Conscripts
@conscripts_active = "conscripts_interaction.dds"			# Activated, Cannot deactivate

# Construction
@can_build_active = "hammer_green.dds"						# Can build + currently building
@cannot_build_active = "hammer_orange.dds"					# Can not build + currently building

# Diplomatic action
@diplomatic_action_active = "diplomatic_actions_blue.dds"	# Activated,Can Deactivate
@diplomatic_action_orange = "diplomatic_actions_orange.dds"	# Activated, Cannot Deactivate

# Diplomaitc play
@diplomatic_play_active = "diplomatic_play.dds"				# Activated, Cannot Deactivate

# Establish Colony
@establish_colony_active = "establish_colony.dds"		 	# Activated, Can Deactivate

# Issue Decree
@issue_decree_active = "issue_decree.dds"					# Activated, Can Deactivate

# Incorporate State
@incorporate_state_active = "incorporate_state_blue.dds"	# Activated, Can Deactivate
@incorporate_state_incorporated = "incorporate_state_orange.dds"	# Activated, Cannot Deactivate (Fully incorporated)

# Move Capital
@move_capital_current = "move_capital.dds"					# Current Capital

# Move Market Capital
@move_market_capital_current = "move_market_capital.dds"	# Current Capital


mm_default = {
	map_painting_mode = countries
	map_painting_mode_alternate = colonizable_provinces
	map_names = countries
	map_markers = { battle_marker construction_marker construction_queue_marker event_marker front_marker city_marker input_shortage_marker state_effect_marker sea_node_marker colony_marker }
	map_tooltip_offset = state

	is_visible = yes
	show_occupation = yes

	gradient_border_settings = default
}

mm_ongoing_diplo_play = {
	map_painting_mode = countries
	map_painting_mode_alternate = colonizable_provinces
	map_names = countries
	map_markers = { battle_marker hq_marker diplomatic_play_marker construction_marker construction_queue_marker event_marker front_marker input_shortage_marker state_effect_marker sea_node_marker }
	map_tooltip_offset = state

	show_occupation = yes

	gradient_border_settings = default
}

mm_strategic_region = {
	map_painting_mode = strategic_regions
	map_names = strategic_regions
	map_markers = { construction_marker construction_queue_marker event_marker sea_node_marker strategic_region_marker diplomatic_play_marker }
	map_tooltip_offset = strategic_region

	is_visible = yes

	gradient_border_settings = default
}

mm_state = {
	map_painting_mode = states
	map_names = states
	map_markers = { construction_marker construction_queue_marker event_marker }

	is_visible = yes

	gradient_border_settings = states
}

mm_theater = {
	map_painting_mode = player_theaters
	map_painting_mode_alternate = colonizable_provinces
	map_names = theaters
	map_markers = { sea_node_marker }
	map_tooltip_offset = theater

	show_occupation = yes

	gradient_border_settings = default
}

mm_military = {
	map_painting_mode = player_alliances
	map_painting_mode_secondary = player_alliances
	map_painting_mode_alternate = player_alliances
	map_markers = { event_marker battle_marker front_marker commander_order_marker hq_marker sea_node_marker }

	gradient_border_settings = mapmode_extra
	is_visible = yes
	show_occupation = yes

	# Texture usage
	use_mapmode_textures = yes

	# Texture slots
	primary_color_red = @this_is_you
	primary_color_green = @war_this_is_you
	primary_color_blue = @war_this_is_you_warleader
	primary_color_alpha = @war_ally
	secondary_color_red = @war_ally_warleader
	secondary_color_green = @war_enemy
	secondary_color_blue = @war_enemy_warleader
	secondary_color_alpha = @color_blue
	alternate_color_red = @color_red
	alternate_color_green = @color_green
}

mm_military_with_past_battles = {
	map_painting_mode = player_alliances
	map_painting_mode_secondary = player_alliances
	map_painting_mode_alternate = player_alliances
	map_markers = { event_marker battle_marker past_battle_marker commander_order_marker hq_marker sea_node_marker }

	gradient_border_settings = mapmode_extra
	is_visible = no
	show_occupation = yes

	# Texture usage
	use_mapmode_textures = yes

	# Texture slots
	primary_color_red = @this_is_you
	primary_color_green = @war_this_is_you
	primary_color_blue = @war_this_is_you_warleader
	primary_color_alpha = @war_ally
	secondary_color_red = @war_ally_warleader
	secondary_color_green = @war_enemy
	secondary_color_blue = @war_enemy_warleader
	secondary_color_alpha = @color_blue
	alternate_color_red = @color_red
	alternate_color_green = @color_green
}

mm_peace_deal = {
	map_painting_mode = player_alliances
	map_painting_mode_secondary = player_alliances
	map_painting_mode_alternate = player_alliances
	map_markers = { event_marker front_marker war_goal_marker }

	gradient_border_settings = mapmode_extra
	is_visible = no
	show_occupation = yes

	# Texture usage
	use_mapmode_textures = yes

	# Texture slots
	primary_color_red = @this_is_you
	primary_color_green = @war_this_is_you
	primary_color_blue = @war_this_is_you_warleader
	primary_color_alpha = @war_ally
	secondary_color_red = @war_ally_warleader
	secondary_color_green = @war_enemy
	secondary_color_blue = @war_enemy_warleader
	secondary_color_alpha = @color_blue
	alternate_color_red = @color_red
	alternate_color_green = @color_green
}

mm_interest_group = {
	map_painting_mode = ig_strength
	map_markers = { interestgroup_marker }

	gradient_border_settings = heatmap
	has_fog_of_war = no

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_population = {
	map_painting_mode = player_population
	map_markers = { }

	gradient_border_settings = heatmap
	has_fog_of_war = no

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_culture = {
	map_painting_mode = culture_population
	map_markers = { homeland_marker }

	gradient_border_settings = heatmap
	has_fog_of_war = no

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_religion = {
	map_painting_mode = religion_population
	map_markers = { }

	gradient_border_settings = heatmap
	has_fog_of_war = no

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_culture_nationally = {
	map_painting_mode = player_culture_population
	map_markers = { homeland_marker }

	gradient_border_settings = heatmap
	has_fog_of_war = no

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_culture_overview = {
	map_painting_mode = culture_overview
	map_markers = { }
	map_names = cultures

	gradient_border_settings = default

	is_visible = yes
	has_fog_of_war = no

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_religion_nationally = {
	map_painting_mode = player_religion_population
	map_markers = { }

	gradient_border_settings = heatmap
	has_fog_of_war = no

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_goods_production = {
	map_painting_mode = goods_production
	map_markers = { state_price_of_goods_marker }

	gradient_border_settings = heatmap
	has_fog_of_war = no

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_goods_consumption = {
	map_painting_mode = goods_consumption
	map_markers = { state_price_of_goods_marker }

	gradient_border_settings = heatmap
	has_fog_of_war = no

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_goods_potentials = {
	map_painting_mode = player_goods_potentials
	# unreachable marker: building_potential_marker
	map_markers = { state_price_of_goods_marker }

	gradient_border_settings = heatmap
	has_fog_of_war = no

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_country_attitude = {
	map_painting_mode = country_attitude
	map_painting_mode_secondary = country_attitude
	map_painting_mode_alternate = country_attitude
	map_markers = { }

	gradient_border_settings = mapmode_extra
	is_visible = yes
	is_visible_to_countryless_observer = no
	show_occupation = no
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes

	# Texture slots
	primary_color_red = @belligerent_domineering
	primary_color_green = @antagonistic_rebellious
	primary_color_blue = @wary
	primary_color_alpha = @disinterested
	secondary_color_red = @cautious
	secondary_color_green = @conciliatory_cooperative
	secondary_color_blue = @loyal_protective
	secondary_color_alpha = @genial
	alternate_color_red = @this_is_you

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_country_relations = {
	map_painting_mode = country_relations
	map_painting_mode_secondary = country_relations
	map_painting_mode_alternate = country_relations
	map_markers = { }

	gradient_border_settings = mapmode_extra
	is_visible = yes
	is_visible_to_countryless_observer = no
	show_occupation = no
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes

	# Texture slots
	primary_color_red = @hostile
	primary_color_green = @cold
	primary_color_blue = @poor
	primary_color_alpha = @cordial
	secondary_color_red = @amicable
	secondary_color_green = @friendly
	secondary_color_blue = @neutral
	alternate_color_red = @this_is_you

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_technology = {
	map_painting_mode = technology_progress
	map_markers = { }

	gradient_border_settings = heatmap
	has_fog_of_war = no

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_trade = {
	map_painting_mode = market_access
	map_names = markets
	map_markers = { market_marker market_trade_route_marker sea_infra_marker state_infra_marker sea_node_marker }

	is_visible = yes

	gradient_border_settings = default
	has_fog_of_war = no

	#soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_province_template_terrain = {
	map_painting_mode = province_terrain
	map_markers = { }

	gradient_border_settings = heatmap
	has_fog_of_war = no

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_theater_interaction = {
	map_painting_mode = player_theaters
	map_painting_mode_alternate = colonizable_provinces
	map_names = theaters
	map_markers = { hq_marker sea_node_marker }

	gradient_border_settings = interactive
	show_occupation = yes
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes
	primary_red_as_gradient_border = yes

	# Texture slots
	secondary_color_red = @color_green			# Can Activate
	secondary_color_alpha = @color_red			# Cannot Activate, but is valid


	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_state_interaction = {
	map_painting_mode = none
	map_names = states
	map_markers = { construction_marker construction_queue_marker building_potential_marker sea_infra_marker state_infra_marker }

	gradient_border_settings = interactive
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes
	primary_red_as_gradient_border = yes

	# Texture slots
	secondary_color_red = @color_green			# Can Activate
	secondary_color_alpha = @color_red			# Cannot Activate, but is valid
	alternate_color_red = @color_orange			# Cannot deactivate

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_country_interaction = {
	map_painting_mode = none
	map_markers = { }

	gradient_border_settings = interactive
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes
	primary_red_as_gradient_border = yes

	# Texture slots
	secondary_color_red = @color_green			# Can Activate
	secondary_color_alpha = @color_red			# Cannot Activate, but is valid

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_market_interaction = {
	map_painting_mode = none
	map_names = markets
	map_markers = { sea_node_marker market_trade_route_marker potential_market_trade_route_marker }

	gradient_border_settings = interactive
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes
	primary_red_as_gradient_border = yes

	# Texture slots
	secondary_color_red = @color_green					# Can Activate
	secondary_color_green = @market_connection_active	# Active Connection
	alternate_color_green = @this_is_you				# This is you (player)

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_strategic_region_interaction = {
	map_painting_mode = none
	map_markers = { sea_node_marker }

	gradient_border_settings = interactive
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes
	primary_red_as_gradient_border = yes

	# Texture slots
	secondary_color_red = @color_green			# Can Activate
	secondary_color_alpha = @color_red			# Cannot Activate, but is valid

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_declare_interest_interaction = {
	map_painting_mode = none
	map_markers = { }

	gradient_border_settings = interactive
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes
	primary_red_as_gradient_border = yes

	# Texture slots
	secondary_color_red = @color_green						# Can Activate
	secondary_color_blue = @declare_interest_active			# Can Deactivate, Has interest
	secondary_color_alpha = @color_red						# Cannot Activate, but is valid
	alternate_color_red = @declare_interest_orange			# Activated, cannot deactivate

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_command_interaction = {
	map_painting_mode = none
	map_markers = { }

	gradient_border_settings = interactive
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes
	primary_red_as_gradient_border = yes

	# Texture slots
	secondary_color_red = @color_green			# Can Activate
	secondary_color_alpha = @color_red			# Cannot Activate, but is valid

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_recruit_generals_interaction = {
	map_painting_mode = none
	map_markers = { }

	gradient_border_settings = interactive
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes
	primary_red_as_gradient_border = yes

	# Texture slots
	secondary_color_red = @color_green			# Can recruit
	secondary_color_alpha = @color_red			# Cannot recruit, but is valid

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_recruit_admirals_interaction = {
	map_painting_mode = none
	map_markers = { }

	gradient_border_settings = interactive
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes
	primary_red_as_gradient_border = yes

	# Texture slots
	secondary_color_red = @color_green			# Can recruit
	secondary_color_alpha = @color_red			# Cannot recruit, but is valid

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_activate_conscripts_interaction = {
	map_painting_mode = none
	map_markers = { }

	gradient_border_settings = interactive
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes
	primary_red_as_gradient_border = yes

	# Texture slots
	secondary_color_red = @color_green					# Can activate
	secondary_color_alpha = @color_red					# Cannot Activate, but is valid
	alternate_color_red = @conscripts_active			# Activated, cannot deactivate

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

# Build buildings mapmode
mm_construction_interaction = {
	map_painting_mode = none
	map_names = states
	map_markers = { construction_marker construction_queue_marker building_potential_marker sea_infra_marker state_infra_marker }

	gradient_border_settings = interactive
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes
	primary_red_as_gradient_border = yes

	# Texture slots
	secondary_color_red = @color_green					# Can build
	secondary_color_green = @can_build_active			# Can build + currently building
	secondary_color_alpha = @color_orange				# Can not build
	alternate_color_green = @cannot_build_active		# Can not build + currently building

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_diplomatic_action_interaction = {
	map_painting_mode = none
	map_markers = { }

	gradient_border_settings = interactive
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes
	primary_red_as_gradient_border = yes

	# Texture slots
	secondary_color_red = @color_green					# Can Activate
	secondary_color_blue = @diplomatic_action_active	# Can Deactivate
	secondary_color_alpha = @color_red					# Cannot Activate
	alternate_color_red = @diplomatic_action_orange		# Active - Cannot Deactivate
	alternate_color_green = @this_is_you				# This is you (player)

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_diplomatic_play_country_interaction = {
	map_painting_mode = none
	map_markers = { }

	gradient_border_settings = interactive
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes
	primary_red_as_gradient_border = yes

	# Texture slots
	secondary_color_red = @color_green				# Can Activate
	secondary_color_alpha = @color_red				# Cannot Activate, but is valid
	alternate_color_red = @diplomatic_play_active	# Activated, Cannot Deactivate

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_diplomatic_play_state_interaction = {
	map_painting_mode = none
	map_names = states
	map_markers = { construction_marker construction_queue_marker building_potential_marker sea_infra_marker state_infra_marker }

	gradient_border_settings = interactive
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes
	primary_red_as_gradient_border = yes

	# Texture slots
	secondary_color_red = @color_green				# Can Activate
	secondary_color_alpha = @color_red				# Cannot Activate, but is valid
	alternate_color_red = @diplomatic_play_active	# Activated, Cannot Deactivate

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_establish_colony_interaction = {
	map_painting_mode = none
	map_markers = { construction_marker construction_queue_marker building_potential_marker sea_node_marker sea_infra_marker state_infra_marker colony_marker }

	gradient_border_settings = interactive
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes
	primary_red_as_gradient_border = yes

	# Texture slots
	secondary_color_red = @color_green				# Can Activate
	secondary_color_blue = @establish_colony_active	# Can Deactivate
	secondary_color_alpha = @color_red				# Cannot Activate, but is valid
}

mm_issue_decree_interaction = {
	map_painting_mode = none
	map_names = states
	map_markers = { construction_marker construction_queue_marker building_potential_marker sea_infra_marker state_infra_marker }

	gradient_border_settings = interactive
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes
	primary_red_as_gradient_border = yes

	# Texture slots
	secondary_color_red = @color_green				# Can Activate
	secondary_color_blue = @issue_decree_active		# Can Deactivate
	secondary_color_alpha = @color_red				# Cannot Activate, but is valid
}

mm_incorporate_state_interaction = {
	map_painting_mode = none
	map_names = states
	map_markers = { construction_marker construction_queue_marker building_potential_marker sea_infra_marker state_infra_marker }

	gradient_border_settings = interactive
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes
	primary_red_as_gradient_border = yes

	# Texture slots
	secondary_color_red = @color_green						# Can Activate
	secondary_color_blue = @incorporate_state_active		# Can Deactivate
	secondary_color_alpha = @color_red						# Cannot Activate, but is valid
	alternate_color_red = @incorporate_state_incorporated	# Active - Cannot Deactivate (Fully incorporated)
}

mm_move_capital_interaction = {
	map_painting_mode = none
	map_names = states
	map_markers = { construction_marker construction_queue_marker building_potential_marker sea_infra_marker state_infra_marker }

	gradient_border_settings = interactive
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes
	primary_red_as_gradient_border = yes

	# Texture slots
	secondary_color_red = @color_green				# Can move
	secondary_color_alpha = @color_red				# Cannot move, but is valid
	alternate_color_red = @move_capital_current		# Current Capital
}

mm_move_market_capital_interaction = {
	map_painting_mode = none
	map_names = states
	map_markers = { construction_marker construction_queue_marker building_potential_marker sea_infra_marker state_infra_marker }

	gradient_border_settings = interactive
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes
	primary_red_as_gradient_border = yes

	# Texture slots
	secondary_color_red = @color_green					# Can move
	secondary_color_alpha = @color_red					# Cannot move, but is valid
	alternate_color_red = @move_market_capital_current	# Current Capital
}

mm_hq_interaction = {
	map_painting_mode = none
	map_markers = { sea_node_marker }

	gradient_border_settings = interactive
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes
	primary_red_as_gradient_border = yes

	# Texture slots
	secondary_color_red = @color_green			# Can activate
	secondary_color_alpha = @color_red			# Cannot activate, but is valid
	alternate_color_red = @color_orange			# Cannot deactivate

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_front_interaction = {
	map_painting_mode = none
	map_markers = { front_marker }

	gradient_border_settings = interactive
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes
	primary_red_as_gradient_border = yes

	# Texture slots
	secondary_color_red = @color_green			# Can activate
	secondary_color_alpha = @color_red			# Cannot activate, but is valid
	alternate_color_red = @color_orange			# Cannot deactivate

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_budget = {
	map_painting_mode = countries
	map_painting_mode_alternate = colonizable_provinces
	map_names = countries
	map_markers = { state_budget_balance_marker }

	show_occupation = yes

	gradient_border_settings = default
}

mm_political = {
	map_painting_mode = clout_nationally
	map_names = states
	map_markers = { interestgroup_marker decree_marker }

	gradient_border_settings = heatmap
	show_occupation = yes

	has_fog_of_war = no
}

mm_loyalists = {
	map_painting_mode = state_gradient
	map_painting_mode_secondary = loyalists
	map_markers = { colony_marker }

	is_visible = yes

	gradient_border_settings = heatmap
	has_fog_of_war = no

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_diplomatic = {
	map_painting_mode = country_attitude
	map_painting_mode_secondary = country_attitude
	map_painting_mode_alternate = country_attitude
	map_markers = { colony_marker diplomatic_play_marker war_marker }

	gradient_border_settings = mapmode_extra
	is_visible = no
	is_visible_to_countryless_observer = no
	show_occupation = yes
	has_fog_of_war = no

	# Texture usage
	use_mapmode_textures = yes

	# Texture slots
	primary_color_red = @belligerent_domineering
	primary_color_green = @antagonistic_rebellious
	primary_color_blue = @wary
	primary_color_alpha = @disinterested
	secondary_color_red = @cautious
	secondary_color_green = @conciliatory_cooperative
	secondary_color_blue = @loyal_protective
	secondary_color_alpha = @genial
	alternate_color_red = @this_is_you

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_standard_of_living = {
	map_painting_mode = state_gradient
	map_painting_mode_secondary = standard_of_living
	map_markers = { colony_marker }

	is_visible = yes

	gradient_border_settings = heatmap
	has_fog_of_war = no

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_radicals = {
	map_painting_mode = state_gradient
	map_painting_mode_secondary = radicals
	map_markers = { colony_marker }

	is_visible = yes

	gradient_border_settings = heatmap
	has_fog_of_war = no

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}

mm_production = {
	map_painting_mode = states
	map_names = states
	map_markers = { construction_marker construction_queue_marker input_shortage_marker employment_marker event_marker production_marker }

	gradient_border_settings = default
}

mm_gdp = {
	map_painting_mode = gdp
	map_names = states
	map_markers = { construction_marker construction_queue_marker event_marker production_marker }
	is_visible = yes

	gradient_border_settings = heatmap
	has_fog_of_war = no
}

mm_gdp_nationally = {
	map_painting_mode = gdp_nationally
	map_names = states
	map_markers = { construction_marker construction_queue_marker event_marker production_marker }
	is_visible = yes
	is_visible_to_countryless_observer = no
	gradient_border_settings = heatmap
	has_fog_of_war = no
}

mm_migration = {
	map_painting_mode = migration
	map_markers = { colony_marker state_migration_target_marker }

	is_visible = yes

	gradient_border_settings = heatmap
	has_fog_of_war = no

	soundeffect = "snapshot:/Dynamic/low_pass_world"
}
