namespace = day_1_events

day_1_events.1 = {
	type = country_event
	hidden = yes
	immediate = {
		
		force_set_literacy_rate = yes
		
		#Enforce ruling IG
		every_country = {
			ruler ?= { 
				interest_group ?= {
					add_ruling_interest_group = yes
				}
			}
		}
		
	}
}