namespace = historical_diplomatic_incidents

# The Iranian Hostage Crisis - PER
historical_diplomatic_incidents.1 = {
    type = country_event

    title = USA_PER_Hostage_Crisis.t 							# Title of the event; ADD LOCALISATION
    desc = USA_PER_Hostage_Crisis.d 							# Description of the event; ADD LOCALISATION
    #flavor = USA_PER_Hostage_Crisis.f 							# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/europenorthamerica_political_extremism.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 		# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 	# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:PER ?= this											# Change TAG of the overlord ****
		game_date >= 1979.11.4									# START DATE: Date colony became self governing ****
		game_date < 1989.11.4									# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_theocratic_government = yes						# Correct gov types
		is_sufficiently_independent = yes						# is_vassal = no		
		is_at_war = no											# war = no
		
		#Cause of unrest
		exists = c:USA
		c:USA = {
			is_a_democracy = yes								# Correct gov types
			is_sufficiently_independent = yes					# is_vassal = no
			is_at_war = no										# war = no
			country_rank = rank_value:super_power
		}
		NOT = { is_subject_of = c:USA }
		
		# Event has not been fired before
		NOT = { has_variable = USA_PER_Hostage_Crisis_hdi }		# CHANGE ****
		
    }
	
	immediate = {
	
		# Fire only once
		set_variable = USA_PER_Hostage_Crisis_hdi				# Change variable ****
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#USA to react
		create_incident = {
			country = c:USA
			value = 5
		} 
		c:USA = { trigger_event = { id = historical_diplomatic_incidents.2 } }
		
		#Domestic reaction
		add_loyalists = {
			value = small_radicals
			culture = cu:persian
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2					# No
		default_option = yes
		
		#Major discontent
		add_radicals = {
			value = very_large_radicals
			interest_group = ig:ig_devout
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# The Iranian Hostage Crisis - USA
historical_diplomatic_incidents.2 = {
    type = country_event
	placement = c:PER

    title = USA_PER_Hostage_Crisis.t 							# Title of the event; ADD LOCALISATION
    desc = USA_PER_Hostage_Crisis.d 							# Description of the event; ADD LOCALISATION
    #flavor = USA_PER_Hostage_Crisis.f 							# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/europenorthamerica_political_extremism.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 		# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 	# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		
    }
	
	immediate = {
		
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#Sanctions
		create_diplomatic_pact = {
			country = c:PER
			type = embargo
		}
		create_diplomatic_pact = {
			country = c:PER
			type = sanction_country
		}
		
		change_relations = { country = c:PER value = -200 }
		
		#National humiliation
		add_modifier = { 
			name = national_humiliation 
			years = 5
			is_decaying = yes 
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2					# No
		default_option = yes
		
		#Attack PER
		create_diplomatic_play = {
			name = USA_Iran_war
			type = dp_make_dominion
			target_country = c:PER
			
			add_war_goal = {
				holder = c:USA
				type = regime_change
				target_country = c:PER
			}
			add_war_goal = {
				holder = c:PER
				type = humiliation
				target_country = c:USA
			}
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# The U-2 Incident - USA
historical_diplomatic_incidents.3 = {
    type = country_event
	placement = c:SOV

    title = USA_SOV_U2_Incident.t 								# Title of the event; ADD LOCALISATION
    desc = USA_SOV_U2_Incident.d 								# Description of the event; ADD LOCALISATION
    #flavor = USA_SOV_U2_Incident.f 							# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/unspecific_airship.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 		# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 	# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:USA ?= this											# Change TAG of the overlord ****
		game_date >= 1960.5.1									# START DATE: Date colony became self governing ****
		game_date < 1970.5.1									# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_democracy = yes									# Correct gov types
		is_sufficiently_independent = yes						# is_vassal = no		
		is_at_war = no											# war = no
		
		country_rank = rank_value:super_power
		
		NOT = { has_law_or_variant = law_type:law_no_espionage }
		
		has_technology_researched = tech_military_airforce_3
		
		#Cause
		exists = c:SOV
		c:SOV = {
			is_a_communist_government = yes						# Correct gov types
			is_sufficiently_independent = yes					# is_vassal = no
			is_at_war = no										# war = no
			country_rank = rank_value:super_power
			
			has_technology_researched = tech_military_support_3
			
			NOT = { is_subject_of = ROOT }
		}
		
		# Event has not been fired before
		NOT = { has_variable = USA_SOV_U2_Incident_hdi }		# CHANGE ****
		
    }
	
	immediate = {
	
		# Fire only once
		set_variable = USA_SOV_U2_Incident_hdi					# Change variable ****
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#SOV to react
		create_incident = {
			country = c:SOV
			value = 5
		} 
		c:SOV = { trigger_event = { id = historical_diplomatic_incidents.4 } }
		
		#Effect of intel
		every_military_formation = { 
			add_organization = 50
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2			# No
		default_option = yes
		
		#National humiliation
		add_modifier = { 
			name = national_humiliation 
			years = 5
			is_decaying = yes 
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# The U-2 Incident - SOV
historical_diplomatic_incidents.4 = {
    type = country_event
	placement = c:USA

    title = USA_SOV_U2_Incident.t 								# Title of the event; ADD LOCALISATION
    desc = USA_SOV_U2_Incident.d 								# Description of the event; ADD LOCALISATION
    #flavor = USA_SOV_U2_Incident.f 							# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/unspecific_airship.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 		# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 	# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		
    }
	
	immediate = {
		
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#Reaction
		change_relations = { country = c:USA value = -100 }
		
		#Prestige from victory!
		add_modifier = { 
			name = national_pride_modifier 
			years = 5
			is_decaying = yes 
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2					# No
		default_option = yes
		
		#Attack USA
		create_diplomatic_play = {
			name = USA_SOV_war
			type = dp_make_dominion
			target_country = c:USA
			
			add_war_goal = {
				holder = ROOT
				type = regime_change
				target_country = c:USA
			}
			add_war_goal = {
				holder = c:USA
				type = make_tributary
				target_country = ROOT
			}
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# Exercise Able Archer 83 - USA
historical_diplomatic_incidents.5 = {
    type = country_event
	placement = c:WGR

    title = USA_SOV_Able_Archer.t 								# Title of the event; ADD LOCALISATION
    desc = USA_SOV_Able_Archer.d 								# Description of the event; ADD LOCALISATION
    #flavor = USA_SOV_Able_Archer.f 							# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/europenorthamerica_before_the_battle.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 		# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 	# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:USA ?= this											# Change TAG of the overlord ****
		game_date >= 1983.11.7									# START DATE: Date colony became self governing ****
		game_date < 1993.11.7									# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_democracy = yes									# Correct gov types
		is_sufficiently_independent = yes						# is_vassal = no		
		is_at_war = no											# war = no
		
		country_rank = rank_value:super_power
		
		has_modifier = nato_leader
		
		#Cause
		exists = c:SOV
		c:SOV = {
			is_a_communist_government = yes						# Correct gov types
			is_sufficiently_independent = yes					# is_vassal = no
			is_at_war = no										# war = no
			country_rank = rank_value:super_power
			NOT = { is_subject_of = ROOT }
			has_modifier = warsaw_pact_leader
			ruler ?= { NOT = { character_is_democratic = yes } }
		}
		exists = c:WGR
		c:WGR = {
			is_subject_of = ROOT
		}
		exists = c:EGR
		c:EGR = {
			is_subject_of = c:SOV
		}
		
		# Event has not been fired before
		NOT = { has_variable = USA_SOV_Able_Archer_hdi }		# CHANGE ****
		
    }
	
	immediate = {
	
		# Fire only once
		set_variable = USA_SOV_Able_Archer_hdi					# Change variable ****
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#SOV to react
		create_incident = {
			country = c:SOV
			value = 5
		} 
		c:SOV = { trigger_event = { id = historical_diplomatic_incidents.6 } }
		
		#Effect of intel
		every_military_formation = { 
			add_organization = 50
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2			# No
		default_option = yes
		
		#National humiliation
		add_modifier = { 
			name = national_humiliation 
			years = 5
			is_decaying = yes 
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# Exercise Able Archer 83 - SOV
historical_diplomatic_incidents.6 = {
    type = country_event
	placement = c:WGR

    title = USA_SOV_Able_Archer.t 								# Title of the event; ADD LOCALISATION
    desc = USA_SOV_Able_Archer.d 								# Description of the event; ADD LOCALISATION
    #flavor = USA_SOV_Able_Archer.f 							# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/europenorthamerica_before_the_battle.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 		# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 			# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		
    }
	
	immediate = {
		
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#Reaction
		change_relations = { country = c:USA value = -100 }
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2					# No
		default_option = yes
		
		#Attack USA
		create_diplomatic_play = {
			name = USA_SOV_war
			type = dp_make_dominion
			target_country = c:USA
			
			add_war_goal = {
				holder = ROOT
				type = regime_change
				target_country = c:USA
			}
			add_war_goal = {
				holder = c:USA
				type = make_tributary
				target_country = ROOT
			}
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# The Korean Air Lines Flight 007 Incident - SOV
historical_diplomatic_incidents.7 = {
    type = country_event

    title = SOV_USA_KOR_Incident.t 								# Title of the event; ADD LOCALISATION
    desc = SOV_USA_KOR_Incident.d 								# Description of the event; ADD LOCALISATION
    #flavor = SOV_USA_KOR_Incident.f 							# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/unspecific_airship.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 	# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 		# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:SOV ?= this											# Change TAG of the overlord ****
		game_date >= 1983.9.1									# START DATE: Date colony became self governing ****
		game_date < 1993.9.1									# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_communist_government = yes							# Correct gov types
		is_sufficiently_independent = yes						# is_vassal = no		
		is_at_war = no											# war = no
		
		country_rank = rank_value:super_power
		
		has_state_in_state_region = STATE_SAKHALIN
		
		NOT = { has_law_or_variant = law_type:law_peasant_levies }
		
		ruler ?= { NOT = { character_is_democratic = yes } }
		
		#Cause
		exists = c:USA
		c:USA = {
			is_a_democracy = yes								# Correct gov types
			is_sufficiently_independent = yes					# is_vassal = no
			is_at_war = no										# war = no
			country_rank = rank_value:super_power
			NOT = { is_subject_of = ROOT }
			relations:root <= relations_threshold:poor
		}
		OR = { 
			AND = { 
				exists = c:SKO
				c:SKO = {
					NOT = { is_a_communist_government = yes }
				}
			}
			AND = { 
				exists = c:NKO
				c:NKO = {
					NOT = { is_a_communist_government = yes }
				}
			}
			AND = { 
				exists = c:KOR
				c:KOR = {
					NOT = { is_a_communist_government = yes }
				}
			}
		}
		OR = { 
			has_global_variable = very_high_world_tension
			has_global_variable = high_world_tension
			historical_railroading_active = yes
		}
		
		# Event has not been fired before
		NOT = { has_variable = SOV_USA_KOR_Incident_hdi }		# CHANGE ****
		
    }
	
	immediate = {
	
		# Fire only once
		set_variable = SOV_USA_KOR_Incident_hdi					# Change variable ****
		
	}
    
    option = {
        name = notice_of_3P_action.option						# Yes
		
		#Diplomatic reaction
		create_incident = {
			country = ROOT
			value = 5
		}
		every_country = { 
			limit = { 
				OR = { 
					c:USA ?= this
					c:KOR ?= this
					c:SKO ?= this
					c:NKO ?= this
				}
				NOT = { is_a_communist_government = yes }
			}
			trigger_event = { id = historical_diplomatic_incidents.8 } 
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }

}

# The Korean Air Lines Flight 007 Incident - USA
historical_diplomatic_incidents.8 = {
    type = country_event

    title = SOV_USA_KOR_Incident.t 								# Title of the event; ADD LOCALISATION
    desc = SOV_USA_KOR_Incident.d 								# Description of the event; ADD LOCALISATION
    #flavor = SOV_USA_KOR_Incident.f 							# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/unspecific_airship.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 	# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 		# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		
    }
	
	immediate = {
		
		
	}
    
    option = {
        name = notice_of_3P_action.option						# Yes
		
		#Reaction
		change_relations = { country = c:SOV value = -50 }
		
    }

}

# Operation PBSuccess
historical_diplomatic_incidents.9 = {
    type = country_event
	placement = c:GUA

    title = USA_GUA_Overthrow.t 								# Title of the event; ADD LOCALISATION
    desc = USA_GUA_Overthrow.d 									# Description of the event; ADD LOCALISATION
    #flavor = USA_GUA_Overthrow.f 								# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/europenorthamerica_political_extremism.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 	# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 		# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:USA ?= this											# Change TAG of the overlord ****
		game_date >= 1954.5.27									# START DATE: Date colony became self governing ****
		game_date < 1964.5.27									# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_democracy = yes									# Correct gov types
		is_sufficiently_independent = yes						# is_vassal = no		
		is_at_war = no											# war = no
		
		country_rank = rank_value:super_power
		
		NOT = { has_law_or_variant = law_type:law_no_espionage }
		
		#Target
		exists = c:GUA
		c:GUA ?= {
			has_variable = GUA_decree_600
			is_a_democracy = yes
			is_sufficiently_independent = yes					# is_vassal = no		
			is_at_war = no										# war = no
		}
		
		#Cause of unrest
		cold_war_ongoing = yes
		
		# Event has not been fired before
		NOT = { has_variable = USA_GUA_Overthrow_hdi }			# CHANGE ****
		
    }
	
	immediate = {
	
		# Fire only once
		set_variable = USA_GUA_Overthrow_hdi					# Change variable ****
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#Infamy
		create_incident = {
			country = c:GUA
			value = 5
		} 
		
		#Mark Guatemala for attack
		c:GUA ?= {
			set_variable = USA_GUA_Overthrow
		}
		change_relations = { 
			country = c:GUA 
			value = -100 
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2			# No
		default_option = yes
		
		#Major discontent
		add_radicals = {
			value = small_radicals
			interest_group = ig:ig_industrialists
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# Operation Storm-333
historical_diplomatic_incidents.10 = {
    type = country_event
	placement = c:AFG

    title = SOV_AFG_Overthrow.t 								# Title of the event; ADD LOCALISATION
    desc = SOV_AFG_Overthrow.d 									# Description of the event; ADD LOCALISATION
    #flavor = SOV_AFG_Overthrow.f 								# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/southamerica_public_figure_assassination.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 	# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 		# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:SOV ?= this											# Change TAG of the overlord ****
		game_date >= 1979.12.27									# START DATE: Date colony became self governing ****
		game_date < 1989.12.27									# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_communist_government = yes							# Correct gov types
		is_sufficiently_independent = yes						# is_vassal = no		
		is_at_war = no											# war = no
		
		country_rank = rank_value:super_power
		
		NOT = { is_neutral = yes }
		
		#Target
		exists = c:AFG
		c:AFG ?= {
			OR = { 
				ruler ?= { has_variable = Hafizullah_Amin }
				AND = {
					ruler ?= { NOT = { has_variable = Nur_Muhammad_Taraki } }
					historical_railroading_active = yes
				}
			}
			is_a_communist_government = yes
			is_sufficiently_independent = yes					# is_vassal = no		
			OR = { 
				has_war_with = ROOT
				is_adjacent_to_country = ROOT
			}
		}
		
		#Cause of unrest
		cold_war_ongoing = yes
		
		# Event has not been fired before
		NOT = { has_variable = SOV_AFG_Overthrow_hdi }			# CHANGE ****
		
    }
	
	immediate = {
	
		# Fire only once
		set_variable = SOV_AFG_Overthrow_hdi					# Change variable ****
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#Kill AFG ruler
		c:AFG ?= { 
			ruler ?= { kill_character = yes } 
			create_character = {
				first_name = "Babrak_Karmal_first_name"
				last_name = "Babrak_Karmal_last_name"
				ruler = yes ig_leader = yes historical = yes
				birth_date = 1929.1.6
				interest_group = ig_trade_unions
				ideology = ideology_autocratic
				traits = {
					reserved
					wrathful
					imperious
				}
				on_created = { set_variable = Babrak_Karmal }
			}
		}
		
		#Infamy
		create_incident = {
			country = c:AFG
			value = 5
		} 
		
		#Mark AFG for attack
		c:AFG ?= {
			set_variable = SOV_AFG_Overthrow
		}
		change_relations = { 
			country = c:AFG
			value = -100 
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2			# No
		default_option = yes
		
		#Major discontent
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_armed_forces
		}
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_intelligentsia
		}
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_trade_unions
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# The Berlin Wall - EGR
historical_diplomatic_incidents.11 = {
    type = country_event

    title = EGR_WGR_Berlin_Wall.t 								# Title of the event; ADD LOCALISATION
    desc = EGR_WGR_Berlin_Wall.d 								# Description of the event; ADD LOCALISATION
    #flavor = EGR_WGR_Berlin_Wall.f 							# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/middleeast_engineer_blueprint.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 		# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 	# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:EGR ?= this											# Change TAG of the overlord ****
		game_date >= 1961.4.13									# START DATE: Date colony became self governing ****
		game_date < 1971.4.13									# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_communist_government = yes							# Correct gov types
		is_sufficiently_independent = yes						# is_vassal = no		
		is_at_war = no											# war = no
		
		#Borders are not closed
		NOT = { has_law_or_variant = law_type:law_closed_borders }
		has_state_in_state_region = STATE_BRANDENBURG
		
		#Cause of unrest
		exists = c:WGR
		c:WGR = {
			is_a_democracy = yes								# Correct gov types
			is_sufficiently_independent = yes					# is_vassal = no
			is_at_war = no										# war = no
			has_state_in_state_region = STATE_BRANDENBURG
		}
		NOT = { is_subject_of = c:WGR }
		
		# Event has not been fired before
		NOT = { has_variable = EGR_WGR_Berlin_Wall_hdi }		# CHANGE ****
		
    }
	
	immediate = {
	
		# Fire only once
		set_variable = EGR_WGR_Berlin_Wall_hdi					# Change variable ****
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#Close borders
		activate_law = law_type:law_closed_borders
		add_treasury = -100000
		set_variable = Berlin_Wall_Constructed
		
		#WGR to react
		change_relations = { country = c:WGR value = -100 }
		create_incident = {
			country = c:WGR
			value = 20
		}
		
		#Domestic reaction
		add_radicals = {
			value = very_large_radicals
			interest_group = ig:ig_petty_bourgeoisie
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2					# No
		default_option = yes
		
		#Major discontent
		add_radicals = {
			value = very_large_radicals
			interest_group = ig:ig_trade_unions
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# Fall of The Berlin Wall - EGR
historical_diplomatic_incidents.12 = {
    type = country_event

    title = EGR_WGR_Berlin_Wall_Fall.t 							# Title of the event; ADD LOCALISATION
    desc = EGR_WGR_Berlin_Wall_Fall.d 							# Description of the event; ADD LOCALISATION
    #flavor = EGR_WGR_Berlin_Wall_Fall.f 						# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/europenorthamerica_political_extremism.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 		# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 	# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:EGR ?= this											# Change TAG of the overlord ****
		game_date >= 1989.11.9									# START DATE: Date colony became self governing ****
		game_date < 1999.11.9									# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_communist_government = yes							# Correct gov types
		is_sufficiently_independent = yes						# is_vassal = no		
		is_at_war = no											# war = no
		
		#Borders are closed
		has_variable = Berlin_Wall_Constructed
		has_law_or_variant = law_type:law_closed_borders
		has_state_in_state_region = STATE_BRANDENBURG
		
		#Cause of unrest
		exists = c:WGR
		c:WGR = {
			is_a_democracy = yes								# Correct gov types
			is_sufficiently_independent = yes					# is_vassal = no
			is_at_war = no										# war = no
			has_state_in_state_region = STATE_BRANDENBURG
		}
		NOT = { is_subject_of = c:WGR }
		
		#Soviet Thaw
		exists = c:SOV
		c:SOV = {
			is_a_communist_government = yes
			is_sufficiently_independent = yes
			is_at_war = no
			ruler ?= { NOT = { character_is_autocratic = yes } }
		}
		
		# Event has not been fired before
		NOT = { has_variable = EGR_WGR_Berlin_Wall_Fall_hdi }	# CHANGE ****
		
    }
	
	immediate = {
	
		# Fire only once
		set_variable = EGR_WGR_Berlin_Wall_Fall_hdi				# Change variable ****
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#Close borders
		activate_law = law_type:law_no_migration_controls
		
		#WGR to react
		change_relations = { country = c:WGR value = 100 }
		
		#Domestic reaction
		add_radicals = {
			value = very_large_radicals
			interest_group = ig:ig_trade_unions
		}
		
		#International reaction
		change_infamy = -10
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2					# No
		default_option = yes
		
		#Major discontent
		add_radicals = {
			value = very_large_radicals
			interest_group = ig:ig_petty_bourgeoisie
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# Bay of Pigs Invasion - USA
historical_diplomatic_incidents.13 = {
    type = country_event
	placement = c:CUB

    title = USA_CUB_Overthrow.t 								# Title of the event; ADD LOCALISATION
    desc = USA_CUB_Overthrow.d 									# Description of the event; ADD LOCALISATION
    #flavor = USA_CUB_Overthrow.f 								# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/middleeast_battlefield_trenches.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 	# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 		# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:USA ?= this											# Change TAG of the overlord ****
		game_date >= 1961.4.17									# START DATE: Date colony became self governing ****
		game_date < 1971.4.17									# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_democracy = yes									# Correct gov types
		is_sufficiently_independent = yes						# is_vassal = no		
		is_at_war = no											# war = no
		
		country_rank = rank_value:super_power
		
		NOT = { has_law_or_variant = law_type:law_no_espionage }
		
		#Target
		exists = c:CUB
		c:CUB ?= {
			is_a_communist_government = yes
			is_sufficiently_independent = yes					# is_vassal = no		
			is_at_war = no										# war = no
		}
		
		# Event has not been fired before
		NOT = { has_variable = USA_CUB_Overthrow_hdi }			# CHANGE ****
		
		#Cause of unrest
		cold_war_ongoing = yes
		
    }
	
	immediate = {
	
		# Fire only once
		set_variable = USA_CUB_Overthrow_hdi					# Change variable ****
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#Infamy
		create_incident = {
			country = c:CUB
			value = 5
		} 
		
		#Mark Cuba for attack
		c:CUB ?= {
			trigger_event = { id = historical_diplomatic_incidents.14 }
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2			# No
		default_option = yes
		
		#Major discontent
		add_radicals = {
			value = small_radicals
			interest_group = ig:ig_industrialists
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# Bay of Pigs Invasion - CUB
historical_diplomatic_incidents.14 = {
    type = country_event

    title = USA_CUB_Overthrow.t 								# Title of the event; ADD LOCALISATION
    desc = USA_CUB_Overthrow.d 									# Description of the event; ADD LOCALISATION
    #flavor = USA_CUB_Overthrow.f 								# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/middleeast_battlefield_trenches.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 	# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 		# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Triggered by event
		
    }
	
	immediate = {
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#Invasion is repelled
		capital ?= {
			state_region = {
				add_devastation = 25
			}
		}
		change_relations = { country = c:USA value = -200 }
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2			# No
		default_option = yes
		
		#Becomes independent
		if = {
			limit = { is_subject = yes }
			make_independent = yes
		}
		
		#Enact new regime
		effect_starting_politics_presidential_republic = yes
		effect_starting_politics_dictatorship = yes
		effect_starting_politics_economic_policy_laissez_faire = yes
		
		#Become a US vassal
		c:USA ?= {
			create_diplomatic_pact = {
				country = c:CUB
				type = puppet
			}
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# Chagos Islands Deportation
historical_diplomatic_incidents.15 = {
    type = country_event
	placement = scope:chagos_islands

    title = GBR_Chagos_Island_deport.t 							# Title of the event; ADD LOCALISATION
    desc = GBR_Chagos_Island_deport.d 							# Description of the event; ADD LOCALISATION
    #flavor = GBR_Chagos_Island_deport.f 						# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/africa_soldiers_breaking_protest.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 		# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 	# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:GBR ?= this											# Change TAG of the overlord ****
		game_date >= 1973.4.27									# START DATE: Date colony became self governing ****
		game_date < 1983.4.27									# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_democracy = yes									# Correct gov types
		is_sufficiently_independent = yes						# is_vassal = no		
		is_at_war = no											# war = no
		
		#Owns Chagos Islands
		p:x5B5C22 ?= {
			owner = ROOT
		}
		
		#Cause of dispute
		exists = c:MRT
		c:MRT = {
			is_sufficiently_independent = yes					# is_vassal = no
			is_at_war = no										# war = no
			NOT = { is_subject_of = ROOT }
		}
		
		# Event has not been fired before
		NOT = { has_variable = GBR_Chagos_Island_deport_hdi }	# CHANGE ****
		
    }
	
	immediate = {
	
		#Mark states
		capital ?= { save_scope_as = capital_scope }
		random_scope_state = {
			limit = { state_region = s:STATE_CEYLON }
			save_scope_as = chagos_islands
		}
	
		# Fire only once
		set_variable = GBR_Chagos_Island_deport_hdi				# Change variable ****
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#Deport Chagos Islanders
		random_scope_state = {
			limit = { state_region = s:STATE_CEYLON }
			every_scope_pop = {
				limit = {
					culture = cu:mauritian
				}
				move_pop = scope:capital_scope
			}
		}
		
		#MRT to react
		random_country = {
			limit = {
				c:MRT ?= this
			}
			change_relations = { country = ROOT value = -100 }
			save_scope_as = MRT_tag
		}
		
		#USA to react
		random_country = {
			limit = {
				c:USA ?= this
				is_a_democracy = yes
				is_sufficiently_independent = yes
				is_at_war = no
				country_rank = rank_value:super_power
			}
			change_relations = { country = ROOT value = 50 }
		}
		
		#Add infamy
		if = {
			limit = { exists = scope:MRT_tag }
			create_incident = {
				country = scope:MRT_tag
				value = 10
			}
		}
		else = {
			change_infamy = 5
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2					# No
		default_option = yes
		
		#USA to react
		random_country = {
			limit = {
				c:USA ?= this
				is_a_democracy = yes
				is_sufficiently_independent = yes
				is_at_war = no
				country_rank = rank_value:super_power
			}
			change_relations = { country = ROOT value = -100 }
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# Project Azorian - USA
historical_diplomatic_incidents.16 = {
    type = country_event
	placement = c:SOV

    title = USA_SOV_Azorian_Incident.t 							# Title of the event; ADD LOCALISATION
    desc = USA_SOV_Azorian_Incident.d 							# Description of the event; ADD LOCALISATION
    #flavor = USA_SOV_Azorian_Incident.f 						# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/unspecific_steam_ship.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 	# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 		# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:USA ?= this											# Change TAG of the overlord ****
		game_date >= 1974.7.4									# START DATE: Date colony became self governing ****
		game_date < 1984.7.4									# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_democracy = yes									# Correct gov types
		is_sufficiently_independent = yes						# is_vassal = no		
		is_at_war = no											# war = no
		
		country_rank = rank_value:super_power
		
		NOT = { has_law_or_variant = law_type:law_no_espionage }
		
		has_technology_researched = tech_sea_infrastructure_4
		
		#Cause
		exists = c:SOV
		c:SOV = {
			is_a_communist_government = yes						# Correct gov types
			is_sufficiently_independent = yes					# is_vassal = no
			is_at_war = no										# war = no
			country_rank = rank_value:super_power
			NOT = { is_subject_of = ROOT }
		}
		
		# Event has not been fired before
		NOT = { has_variable = USA_SOV_Azorian_Incident_hdi }	# CHANGE ****
		
    }
	
	immediate = {
	
		# Fire only once
		set_variable = USA_SOV_Azorian_Incident_hdi				# Change variable ****
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#SOV to react
		create_incident = {
			country = c:SOV
			value = 5
		} 
		c:SOV = { trigger_event = { id = historical_diplomatic_incidents.17 } }
		
		#Effect of intel
		every_military_formation = { 
			add_organization = 50
		}
		
		#Gain Prestige
		add_modifier = { 
			name = small_national_pride_modifier
			years = 5
			is_decaying = yes 
		}
		
		#Domestic Reaction
		add_loyalists = {
			value = large_radicals
			interest_group = ig:ig_armed_forces
		}
		add_loyalists = {
			value = large_radicals
			interest_group = ig:ig_intelligentsia
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2			# No
		default_option = yes
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# Project Azorian - SOV
historical_diplomatic_incidents.17 = {
    type = country_event
	placement = c:USA

    title = USA_SOV_Azorian_Incident_1.t 							# Title of the event; ADD LOCALISATION
    desc = USA_SOV_Azorian_Incident.d 							# Description of the event; ADD LOCALISATION
    #flavor = USA_SOV_Azorian_Incident.f 						# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/unspecific_steam_ship.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 	# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 		# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Triggered by event
		
    }
	
	immediate = {
		
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#Reaction
		change_relations = { country = c:USA value = -100 }
		
		#Lose Prestige
		add_modifier = { 
			name = small_national_humiliation
			years = 5
			is_decaying = yes 
		}
		
		#Domestic Reaction
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_armed_forces
		}
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_intelligentsia
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2					# No
		default_option = yes
		
		#Attack USA
		create_diplomatic_play = {
			name = USA_SOV_war
			type = dp_make_dominion
			target_country = c:USA
			
			add_war_goal = {
				holder = ROOT
				type = regime_change
				target_country = c:USA
			}
			add_war_goal = {
				holder = c:USA
				type = make_tributary
				target_country = ROOT
			}
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# The Reagan Doctrine - USA
historical_diplomatic_incidents.18 = {
    type = country_event

    title = USA_Reagan_Doctrine.t 								# Title of the event; ADD LOCALISATION
    desc = USA_Reagan_Doctrine.d 								# Description of the event; ADD LOCALISATION
    #flavor = USA_Reagan_Doctrine.f 							# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/unspecific_military_parade.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 	# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 		# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:USA ?= this											# Change TAG of the overlord ****
		game_date >= 1985.2.6									# START DATE: Date colony became self governing ****
		game_date < 1995.2.6									# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_democracy = yes									# Correct gov types
		is_sufficiently_independent = yes						# is_vassal = no		
		is_at_war = no											# war = no
		
		country_rank = rank_value:super_power
		
		ruler ?= { 
			OR = { 
				has_variable = Ronald_Reagan
				AND = { 
					historical_railroading_active = yes
					interest_group = { is_interest_group_type = ig_industrialists } 
				}
			}
		}
		
		#Cause
		exists = c:SOV
		c:SOV = {
			is_a_communist_government = yes						# Correct gov types
			is_sufficiently_independent = yes					# is_vassal = no
			is_at_war = no										# war = no
			country_rank = rank_value:super_power
			NOT = { is_subject_of = ROOT }
		}
		
		# Event has not been fired before
		NOT = { has_variable = USA_Reagan_Doctrine_hdi }		# CHANGE ****
		
    }
	
	immediate = {
	
		# Fire only once
		set_variable = USA_Reagan_Doctrine_hdi					# Change variable ****
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#Enact new regime
		#activate_law = law_type:law_offensive_war
		activate_law = law_type:law_globalist_fp
		activate_law = law_type:law_full_free_speech
		activate_law = law_type:law_offensive_espionage
		activate_law = law_type:law_wmd_allowed
		
		#Reaction
		change_relations = { country = c:SOV value = -100 }
		
		#Domestic Reaction
		add_loyalists = {
			value = large_radicals
			interest_group = ig:ig_armed_forces
		}
		add_loyalists = {
			value = large_radicals
			interest_group = ig:ig_intelligentsia
		}
		
		#Power Bloc
		if = {
			limit = { 
				is_power_bloc_leader = yes 
				power_bloc ?= { NOT = { has_principle = principle:principle_military_industry_3 } }
			}
			power_bloc ?= { 
				add_principle = principle_military_industry_3
			}
		}
		
		#Infamy
		change_infamy = 10
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2			# No
		default_option = yes
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# The Brezhnev Doctrine - SOV
historical_diplomatic_incidents.19 = {
    type = country_event

    title = SOV_Brezhnev_Doctrine.t 							# Title of the event; ADD LOCALISATION
    desc = SOV_Brezhnev_Doctrine.d 								# Description of the event; ADD LOCALISATION
    #flavor = SOV_Brezhnev_Doctrine.f 							# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/unspecific_military_parade.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 	# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 		# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:SOV ?= this											# Change TAG of the overlord ****
		game_date >= 1968.8.20									# START DATE: Date colony became self governing ****
		game_date < 1978.8.20									# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_communist_government = yes							# Correct gov types
		is_sufficiently_independent = yes						# is_vassal = no		
		is_at_war = no											# war = no
		
		country_rank = rank_value:super_power
		
		ruler ?= { 
			OR = { 
				has_variable = Leonid_Brezhnev
				AND = { 
					character_is_autocratic = yes
					interest_group = { is_interest_group_type = ig_trade_unions } 
				}
			}
		}
		
		#Cause
		exists = c:USA
		c:USA = {
			is_a_democracy = yes								# Correct gov types
			is_sufficiently_independent = yes					# is_vassal = no
			is_at_war = no										# war = no
			country_rank = rank_value:super_power
			NOT = { is_subject_of = ROOT }
		}
		
		# Event has not been fired before
		NOT = { has_variable = SOV_Brezhnev_Doctrine_hdi }		# CHANGE ****
		
    }
	
	immediate = {
	
		# Fire only once
		set_variable = SOV_Brezhnev_Doctrine_hdi				# Change variable ****
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#Enact new regime
		#activate_law = law_type:law_offensive_war
		activate_law = law_type:law_globalist_fp
		activate_law = law_type:law_outlawed_dissent
		activate_law = law_type:law_offensive_espionage
		activate_law = law_type:law_wmd_allowed
		
		#Reaction
		change_relations = { country = c:USA value = -100 }
		
		#Domestic Reaction
		add_loyalists = {
			value = large_radicals
			interest_group = ig:ig_armed_forces
		}
		add_loyalists = {
			value = large_radicals
			interest_group = ig:ig_intelligentsia
		}
		
		#Power Bloc
		if = {
			limit = { 
				is_power_bloc_leader = yes 
				power_bloc ?= { NOT = { has_principle = principle:principle_police_coordination_3 } }
			}
			power_bloc ?= { 
				add_principle = principle_police_coordination_3
			}
		}
		
		#Infamy
		change_infamy = 10
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2			# No
		default_option = yes
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# Atlanticism - GBR
historical_diplomatic_incidents.20 = {
    type = country_event
	placement = c:USA

    title = GBR_Atlanticism.t 									# Title of the event; ADD LOCALISATION
    desc = GBR_Atlanticism.d 									# Description of the event; ADD LOCALISATION
    #flavor = GBR_Atlanticism.f 								# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/europenorthamerica_capitalists_meeting.bk2" }

    icon = "gfx/interface/icons/invention_icons/democracy.dds" 		# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 	# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:GBR ?= this											# Change TAG of the overlord ****
		game_date >= 1985.1.1									# START DATE: Date colony became self governing ****
		game_date < 1995.1.1									# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_democracy = yes									# Correct gov types
		is_sufficiently_independent = yes						# is_vassal = no		
		is_at_war = no											# war = no
		
		NOT = { country_rank = rank_value:super_power }
		is_subject = no
		
		ruler ?= { 
			has_variable = Margaret_Thatcher
		}
		
		#Cause
		exists = c:USA
		c:USA = {
			is_a_democracy = yes								# Correct gov types
			is_sufficiently_independent = yes					# is_vassal = no
			is_at_war = no										# war = no
			country_rank = rank_value:super_power
			NOT = { is_subject_of = ROOT }
			ruler ?= { 
				has_variable = Ronald_Reagan
			}
		}
		exists = c:SOV
		c:SOV = {
			is_a_communist_government = yes						# Correct gov types
			is_sufficiently_independent = yes					# is_vassal = no
			is_at_war = no										# war = no
			country_rank = rank_value:super_power
			NOT = { is_subject_of = ROOT }
		}
		
		#Not already having a special relationship
		OR = { 
			NOT = { any_scope_treaty = { binds = ROOT any_scope_article = { has_type = foreign_investment_rights } } }
			NOT = { any_scope_treaty = { binds = ROOT any_scope_article = { has_type = alliance } } }
			NOT = { any_scope_treaty = { binds = ROOT any_scope_article = { has_type = trade_privilege } } }
		}
		
		# Event has not been fired before
		NOT = { has_variable = GBR_Atlanticism_hdi }			# CHANGE ****
		
    }
	
	immediate = {
	
		# Fire only once
		set_variable = GBR_Atlanticism_hdi						# Change variable ****
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#Antagonise Russia
		create_incident = {
			country = c:SOV
			value = 10
		}
		
		#Become Client State of USA
		c:USA = { trigger_event = { id = historical_diplomatic_incidents.21 } }
		
		#Domestic Reaction
		add_loyalists = {
			value = large_radicals
			interest_group = ig:ig_armed_forces
		}
		add_loyalists = {
			value = large_radicals
			interest_group = ig:ig_intelligentsia
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2			# No
		default_option = yes
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# Atlanticism - USA
historical_diplomatic_incidents.21 = {
    type = country_event
	placement = c:GBR

    title = GBR_Atlanticism_1.t 									# Title of the event; ADD LOCALISATION
    desc = GBR_Atlanticism.d 									# Description of the event; ADD LOCALISATION
    #flavor = GBR_Atlanticism.f 								# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/europenorthamerica_capitalists_meeting.bk2" }

    icon = "gfx/interface/icons/invention_icons/democracy.dds" 		# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 	# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Triggered by event
		
    }
	
	immediate = {
		
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#UK becomes Client State of USA
		set_relations = { country = c:GBR value = 100 }
		create_treaty = {
			name = treaty_name_USA_GBR_alliance
			first_country = c:USA
			second_country = c:GBR

			is_draft = no
			binding_period = { years = 10 }

			articles_to_create = {
				{
					article = alliance
				}
				{
					article = foreign_investment_rights
					source_country = c:GBR
					target_country = c:USA
				}
				{
					article = trade_privilege
					source_country = c:GBR
					target_country = c:USA
				}
				{
					article = military_assistance
					source_country = c:USA
					target_country = c:GBR
				}
			}
		}
		
		#Domestic Reaction
		add_loyalists = {
			value = large_radicals
			interest_group = ig:ig_armed_forces
		}
		add_loyalists = {
			value = large_radicals
			interest_group = ig:ig_intelligentsia
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2			# No
		default_option = yes
		
		change_relations = { country = c:GBR value = -50 }
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# Death of Dag Hammarskjold
historical_diplomatic_incidents.22 = {
    type = country_event
	placement = c:CNG

    title = NOR_Dag_Hammarskjold_death.t 							# Title of the event; ADD LOCALISATION
    desc = NOR_Dag_Hammarskjold_death.d 							# Description of the event; ADD LOCALISATION
    #flavor = NOR_Dag_Hammarskjold_death.f 							# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/unspecific_fire.bk2" }

    icon = "gfx/interface/icons/invention_icons/event_newspaper.dds" 	# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 		# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {														# What triggers the event, if using other events or journal entries leave empty
		
		c:SWE ?= this
		game_date >= 1961.9.18
		game_date < 1971.9.18
		
		#Congo Civil War ongoing
		exists = c:CNG
		exists = c:FCG
		c:CNG = { has_war_with = c:FCG }
		
		#UNSG
		any_scope_character = { 
			has_variable = Dag_Hammarskjold
			has_modifier = unsg_character
		}
		
    }
	
	immediate = {
		
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1				# Yes
        highlighted_option = yes
		
		#Diplomatic impact
		change_relations = { country = c:CNG value = -100 }
		change_relations = { country = c:FCG value = -100 }
		
		#Kill Character
		random_scope_character = {
			limit = { has_variable = Dag_Hammarskjold }
			kill_character = yes
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2				# No
		default_option = yes
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# The 1958 Lebanon crisis - LEB
historical_diplomatic_incidents.23 = {
    type = country_event

    title = LEB_Crisis_1958.t 									# Title of the event; ADD LOCALISATION
    desc = LEB_Crisis_1958.d 									# Description of the event; ADD LOCALISATION
    #flavor = LEB_Crisis_1958.f 								# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/europenorthamerica_capitalists_meeting.bk2" }

    icon = "gfx/interface/icons/invention_icons/democracy.dds" 		# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 	# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:LEB ?= this											# Change TAG of the overlord ****
		game_date >= 1958.7.15									# START DATE: Date colony became self governing ****
		game_date < 1968.7.15									# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_democracy = yes									# Correct gov types
		is_sufficiently_independent = yes						# is_vassal = no		
		is_at_war = no											# war = no
		
		ruler ?= { 
			has_variable = Camille_Chamoun
		}
		
		#Cause
		exists = c:UAR
		c:UAR = {
			is_sufficiently_independent = yes
			is_adjacent_to_country = ROOT
		}
		exists = c:USA
		c:USA = {
			is_a_democracy = yes								# Correct gov types
			is_sufficiently_independent = yes					# is_vassal = no
			is_at_war = no										# war = no
			country_rank = rank_value:super_power
			NOT = { is_subject_of = ROOT }
		}
		exists = c:SOV
		c:SOV = {
			is_a_communist_government = yes						# Correct gov types
			is_sufficiently_independent = yes					# is_vassal = no
			is_at_war = no										# war = no
			country_rank = rank_value:super_power
			NOT = { is_subject_of = ROOT }
		}
		
		# Event has not been fired before
		NOT = { has_variable = LEB_Crisis_1958_hdi }			# CHANGE ****
		
    }
	
	immediate = {
	
		# Fire only once
		set_variable = LEB_Crisis_1958_hdi						# Change variable ****
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#Become Client State of USA
		c:USA = { trigger_event = { id = historical_diplomatic_incidents.24 } }
		
		#New regime
		activate_law = law_type:law_no_term_limit
		
		#Domestic Reaction
		add_loyalists = {
			value = large_radicals
			interest_group = ig:ig_armed_forces
		}
		add_loyalists = {
			value = large_radicals
			interest_group = ig:ig_intelligentsia
		}
		add_radicals = {
			value = large_radicals
			religion = rel:sunni
		}
		add_radicals = {
			value = large_radicals
			religion = rel:shiite
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2			# No
		default_option = yes
		
		#New Ruler
		ruler = { kill_character = yes }
		
		#Gain relations with UAR
		set_relations = { country = c:UAR value = 100 }
		
		#Domestic unrest
		add_radicals = {
			value = large_radicals
			religion = rel:catholic
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# The 1958 Lebanon crisis - USA
historical_diplomatic_incidents.24 = {
    type = country_event
	placement = c:LEB

    title = LEB_Crisis_1958.t 									# Title of the event; ADD LOCALISATION
    desc = LEB_Crisis_1958.d 									# Description of the event; ADD LOCALISATION
    #flavor = LEB_Crisis_1958.f 								# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/europenorthamerica_capitalists_meeting.bk2" }

    icon = "gfx/interface/icons/invention_icons/democracy.dds" 		# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 	# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Triggered by event
		
    }
	
	immediate = {
		
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#LEB becomes Client State of USA
		if = { 
			limit = { is_subject = yes } 
			make_independent = yes 
		}
		set_relations = { country = c:USA value = 100 }
		create_diplomatic_pact = {
			country = c:LEB
			type = protectorate
		}
		
		#Antagonise Russia
		create_incident = {
			country = c:SOV
			value = 10
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2			# No
		default_option = yes
		
		change_relations = { country = c:LEB value = -50 }
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# The Fatti di Rovereta
historical_diplomatic_incidents.25 = {
    type = country_event
	placement = scope:san_marino_location

    title = ITA_SAN_1957.t 										# Title of the event; ADD LOCALISATION
    desc = ITA_SAN_1957.d 										# Description of the event; ADD LOCALISATION
    #flavor = ITA_SAN_1957.f 									# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/europenorthamerica_political_extremism.bk2" }

    icon = "gfx/interface/icons/event_icons/event_protest.dds" 		# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 	# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:ITA ?= this											# Change TAG of the overlord ****
		game_date >= 1957.9.19									# START DATE: Date colony became self governing ****
		game_date < 1967.9.19									# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_democracy = yes									# Correct gov types
		is_sufficiently_independent = yes						# is_vassal = no		
		is_at_war = no											# war = no
		
		owns_entire_state_region = STATE_ROMAGNA
		
		#Cause
		exists = c:USA
		c:USA = {
			is_a_democracy = yes								# Correct gov types
			is_sufficiently_independent = yes					# is_vassal = no
			is_at_war = no										# war = no
			country_rank = rank_value:super_power
			NOT = { is_subject_of = ROOT }
			right_wing_ruling_coalition = yes
		}
		exists = c:SOV
		c:SOV = {
			is_a_communist_government = yes						# Correct gov types
			is_sufficiently_independent = yes					# is_vassal = no
			is_at_war = no										# war = no
			country_rank = rank_value:super_power
			NOT = { is_subject_of = ROOT }
		}
		
		# Event has not been fired before
		NOT = { has_variable = ITA_SAN_1957_hdi }				# CHANGE ****
		
    }
	
	immediate = {
		
		random_scope_state = {
			limit = { state_region = s:STATE_ROMAGNA }
			save_scope_as = san_marino_location
		}
	
		# Fire only once
		set_variable = ITA_SAN_1957_hdi							# Change variable ****
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#Diplomatic impact
		every_country = {
			limit = { 
				OR = { has_modifier = warsaw_pact_member has_modifier = warsaw_pact_leader }
				is_sufficiently_independent = yes
			}
			change_relations = { country = ROOT value = -100 }
		}
		every_country = {
			limit = { 
				OR = { has_modifier = nato_member has_modifier = nato_leader }
				is_sufficiently_independent = yes
				country_rank >= rank_value:major_power
			}
			change_relations = { country = ROOT value = 50 }
		}
		
		change_infamy = 5
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2				# No
		default_option = yes
		
		#Diplomatic impact
		every_country = {
			limit = { 
				OR = { has_modifier = nato_member has_modifier = nato_leader }
				is_sufficiently_independent = yes
				country_rank >= rank_value:major_power
			}
			change_relations = { country = ROOT value = -50 }
		}
		every_country = {
			limit = { 
				OR = { has_modifier = warsaw_pact_member has_modifier = warsaw_pact_leader }
				is_sufficiently_independent = yes
			}
			change_relations = { country = ROOT value = 50 }
		}
		
		#Domestic reaction
		if = {
			limit = { NOT = { any_political_movement = { is_political_movement_type = movement_communist } } }
			create_political_movement = { type = movement_communist }
		}
		add_modifier = { 
			name = modifier_communist_peasant_agitation 
			years = 5 
			is_decaying = yes
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# The Kargil War, 1999
historical_diplomatic_incidents.26 = {
    type = country_event
	placement = scope:HND_Kashmir

    title = HND_PAK_KAS_1999.t 									# Title of the event; ADD LOCALISATION
    desc = HND_PAK_KAS_1999.d 									# Description of the event; ADD LOCALISATION
    #flavor = HND_PAK_KAS_1999.f 								# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/middleeast_battlefield_trenches.bk2" }

    icon = "gfx/interface/icons/event_icons/event_protest.dds" 		# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 	# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:PAK ?= this											# Change TAG of the overlord ****
		game_date >= 1999.5.3									# START DATE: Date colony became self governing ****
		game_date < 2009.5.3									# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_democracy = yes									# Correct gov types
		is_sufficiently_independent = yes						# is_vassal = no		
		is_at_war = no											# war = no
		
		OR = { has_state_in_state_region = STATE_KASHMIR any_subject_or_below = { has_state_in_state_region = STATE_KASHMIR } }
		
		is_not_neutral = yes
		
		#Cause
		exists = c:HND
		c:HND = {
			is_a_democracy = yes								# Correct gov types
			is_sufficiently_independent = yes					# is_vassal = no
			is_at_war = no										# war = no
			NOT = { is_subject_of = ROOT }
			OR = { has_state_in_state_region = STATE_KASHMIR any_subject_or_below = { has_state_in_state_region = STATE_KASHMIR } }
		}
		
		# Event has not been fired before
		NOT = { has_variable = HND_PAK_KAS_1999_hdi }			# CHANGE ****
		
    }
	
	immediate = {
		
		random_state = {
			limit = { state_region = s:STATE_KASHMIR owner = { NOR = { this = root is_subject_of = ROOT } } }
			save_scope_as = HND_Kashmir
		}
		random_state = {
			limit = { state_region = s:STATE_KASHMIR owner = { OR = { this = root is_subject_of = ROOT } } }
			save_scope_as = PAK_Kashmir
		}
		
		#Add claim
		s:STATE_KASHMIR = { add_claim = c:PAK add_claim = c:HND }
	
		# Fire only once
		set_variable = HND_PAK_KAS_1999_hdi						# Change variable ****
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#Diplomatic impact
		change_relations = { country = c:HND value = -100 }
		create_incident = {
			country = c:PAK
			value = 25
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2				# No
		default_option = yes
		
		# War
		if = {
			limit = { scope:HND_Kashmir.owner ?= { NOT = { this = c:HND } is_subject_of = c:HND } }
			create_diplomatic_play = {
				name = second_indo_pakistan_war
				type = dp_annex_war_historical
				target_state = scope:HND_Kashmir
				
				war = yes

				add_war_goal = {
					holder = c:HND
					type = return_state
					target_state = scope:PAK_Kashmir
				}
			}
		}
		else = {
			create_diplomatic_play = {
				name = second_indo_pakistan_war
				type = dp_return_state_historical
				target_state = scope:HND_Kashmir
				
				war = yes

				add_war_goal = {
					holder = c:HND
					type = return_state
					target_state = scope:PAK_Kashmir
				}
			}
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# India–Pakistan Crisis, 2025
historical_diplomatic_incidents.27 = {
    type = country_event
	placement = scope:HND_Kashmir

    title = HND_PAK_KAS_2025.t 									# Title of the event; ADD LOCALISATION
    desc = HND_PAK_KAS_2025.d 									# Description of the event; ADD LOCALISATION
    #flavor = HND_PAK_KAS_2025.f 								# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/middleeast_battlefield_trenches.bk2" }

    icon = "gfx/interface/icons/event_icons/event_protest.dds" 		# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 	# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:HND ?= this											# Change TAG of the overlord ****
		game_date >= 2025.4.23									# START DATE: Date colony became self governing ****
		game_date < 2035.4.23									# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_democracy = yes									# Correct gov types
		is_sufficiently_independent = yes						# is_vassal = no		
		is_at_war = no											# war = no
		
		OR = { has_state_in_state_region = STATE_KASHMIR any_subject_or_below = { has_state_in_state_region = STATE_KASHMIR } }
		
		is_not_neutral = yes
		
		#Cause
		exists = c:PAK
		c:PAK = {
			is_a_democracy = yes								# Correct gov types
			is_sufficiently_independent = yes					# is_vassal = no
			is_at_war = no										# war = no
			NOT = { is_subject_of = ROOT }
			OR = { has_state_in_state_region = STATE_KASHMIR any_subject_or_below = { has_state_in_state_region = STATE_KASHMIR } }
		}
		
		# Event has not been fired before
		NOT = { has_variable = HND_PAK_KAS_2025_hdi }			# CHANGE ****
		
    }
	
	immediate = {
		
		random_state = {
			limit = { state_region = s:STATE_KASHMIR owner = { NOR = { this = root is_subject_of = ROOT } } }
			save_scope_as = PAK_Kashmir
		}
		random_state = {
			limit = { state_region = s:STATE_KASHMIR owner = { OR = { this = root is_subject_of = ROOT } } }
			save_scope_as = HND_Kashmir
		}
		
		#Add claim
		s:STATE_KASHMIR = { add_claim = c:PAK add_claim = c:HND }
	
		# Fire only once
		set_variable = HND_PAK_KAS_2025_hdi						# Change variable ****
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#Diplomatic impact
		change_relations = { country = c:PAK value = -100 }
		create_incident = {
			country = c:HND
			value = 10
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2				# No
		default_option = yes
		
		# War
		if = {
			limit = { scope:HND_Kashmir.owner ?= { NOT = { this = c:HND } is_subject_of = c:HND } }
			create_diplomatic_play = {
				name = second_indo_pakistan_war
				type = dp_return_state_historical
				target_state = scope:PAK_Kashmir
				
				war = yes

				add_war_goal = {
					holder = c:PAK
					type = annex_country
					target_state = scope:HND_Kashmir
				}
			}
		}
		else = {
			create_diplomatic_play = {
				name = second_indo_pakistan_war
				type = dp_return_state_historical
				target_state = scope:PAK_Kashmir
				
				war = yes

				add_war_goal = {
					holder = c:PAK
					type = return_state
					target_state = scope:HND_Kashmir
				}
			}
		}
		
		#Diplomatic impact
		create_incident = {
			country = c:HND
			value = 20
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# The Vlora incident - ALB
historical_diplomatic_incidents.28 = {
    type = country_event

    title = SOV_ALB_Vlora_Incident.t 							# Title of the event; ADD LOCALISATION
    desc = SOV_ALB_Vlora_Incident.d 							# Description of the event; ADD LOCALISATION
    #flavor = SOV_ALB_Vlora_Incident.f 							# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/middleeast_battlefield_trenches.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 	# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 		# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:ALB ?= this											# Change TAG of the overlord ****
		game_date >= 1961.6.8									# START DATE: Date colony became self governing ****
		game_date < 1971.6.8									# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_communist_government = yes							# Correct gov types
		is_sufficiently_independent = yes						# is_vassal = no		
		is_at_war = no											# war = no
		
		#Hoxha
		ruler ?= { 
			character_is_autocratic = yes 
			interest_group = { is_interest_group_type = ig_trade_unions } 
		}
		
		#Cause
		has_global_variable = sino_soviet_split_global_var
		exists = c:SOV
		c:SOV ?= { 												# Change TAG of the colony ****
			
			is_a_communist_government = yes
			is_sufficiently_independent = yes
			is_at_war = no
			
			NOT = { is_direct_subject_of = ROOT }
			any_scope_treaty = { binds = ROOT any_scope_article = { OR = { has_type = defensive_pact has_type = alliance } } }
			
			#Triggered by Khrushchev
			OR = {
				AND = { 
					exists = ruler
					ruler ?= { interest_group = { is_interest_group_type = ig_rural_folk } is_historical = yes }
				}
				NOT = { has_modifier = communist_ruling_party_ideology }
			}
			
		}
		is_direct_subject_of = c:SOV
		
		# Event has not been fired before
		NOT = { has_variable = SOV_ALB_Vlora_Incident_hdi }		# CHANGE ****
		
    }
	
	immediate = {
	
		# Fire only once
		set_variable = SOV_ALB_Vlora_Incident_hdi				# Change variable ****
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#Choice for USSR
		c:SOV ?= {
			trigger_event = { id = historical_diplomatic_incidents.29 }
		}
		
		#Gain relations with China
		every_country = {
			limit = {
				OR = { c:PRC ?= this c:CHI ?= this }
				is_a_communist_government = yes
				is_sufficiently_independent = yes
			}
			change_relations = { 
				country = ROOT
				value = 200 
			}
			#AI
			hidden_effect = {
				set_mutual_secret_goal = {
					country = ROOT
					secret_goal = befriend
				}
			}
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2			# No
		default_option = yes
		
		#Domestic effects
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_armed_forces
		}
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_trade_unions
		}
		add_modifier = {
			name = weakened_government_authority
			years = 10
			is_decaying = yes
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# The Vlora incident - SOV
historical_diplomatic_incidents.29 = {
    type = country_event
	placement = c:ALB

    title = SOV_ALB_Vlora_Incident.t 							# Title of the event; ADD LOCALISATION
    desc = SOV_ALB_Vlora_Incident.d 							# Description of the event; ADD LOCALISATION
    #flavor = SOV_ALB_Vlora_Incident.f 							# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/middleeast_battlefield_trenches.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 	# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 		# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Triggered by event
		
    }
	
	immediate = {
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#Albania becomes independent
		c:ALB ?= {
			
			make_independent = yes
			
			#Prestige from being independent of USSR
			add_modifier = { 
				name = national_pride_modifier 
				months = 60 
				is_decaying = yes 
			}
			
		}
		
		# Lose relations with ALB
		change_relations = { 
			country = c:ALB
			value = -100
		}
		
		#AI
		hidden_effect = {
			set_mutual_secret_goal = {
				country = c:ALB
				secret_goal = antagonize
			}
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2			# No
		default_option = yes
		
		#Albania becomes independent
		c:ALB ?= {
			make_independent = yes
		}
		
		#War
		create_diplomatic_play = {
			name = ALB_SOV_war
			type = dp_make_puppet
			target_country = c:ALB
			
			#war = yes
			
			#ALB's Goals
			add_war_goal = {
				holder = c:ALB
				type = humiliation
				target_country = c:SOV
			}
		}
		
		#AI
		hidden_effect = {
			set_mutual_secret_goal = {
				country = c:ALB
				secret_goal = antagonize
			}
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# The Sri Lankan Northern Offensive - SRL
historical_diplomatic_incidents.30 = {
    type = country_event
	placement = c:TAE

    title = SRL_TAE_civil_war_end.t 							# Title of the event; ADD LOCALISATION
    desc = SRL_TAE_civil_war_end.d 								# Description of the event; ADD LOCALISATION
    #flavor = SRL_TAE_civil_war_end.f 							# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/europenorthamerica_political_extremism.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 	# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 		# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:SRL ?= this											# Change TAG of the overlord ****
		game_date >= 2009.5.17									# START DATE: Date colony became self governing ****
		game_date < 2019.5.17									# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_democracy = yes									# Correct gov types
		is_sufficiently_independent = yes						# is_vassal = no		
		is_at_war = no											# war = no
		
		#Cause of unrest
		exists = c:TAE
		c:TAE ?= {
			is_sufficiently_independent = yes					# is_vassal = no
			is_at_war = no										# war = no
			NOT = { is_subject_of = ROOT }
		}
		NOT = { is_subject_of = c:TAE }
		
		# Event has not been fired before
		NOT = { has_variable = SRL_TAE_civil_war_end_hdi }		# CHANGE ****
		
    }
	
	immediate = {
	
		# Fire only once
		set_variable = SRL_TAE_civil_war_end_hdi				# Change variable ****
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#TAE to react
		c:TAE = { trigger_event = { id = historical_diplomatic_incidents.31 } }
		
		#International reaction
		create_incident = {
			country = c:TAE
			value = 25
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2					# No
		default_option = yes
		
		#Major discontent
		add_radicals = {
			value = very_large_radicals
			culture = cu:sinhala
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# The Sri Lankan Northern Offensive - TAE
historical_diplomatic_incidents.31 = {
    type = country_event
	placement = c:TAE

    title = SRL_TAE_civil_war_end.t 							# Title of the event; ADD LOCALISATION
    desc = SRL_TAE_civil_war_end.d 								# Description of the event; ADD LOCALISATION
    #flavor = SRL_TAE_civil_war_end.f 							# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/europenorthamerica_political_extremism.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 		# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 	# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		
    }
	
	immediate = {
		
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#Reintegrate into SRL
		c:SRL ?= {
			activate_law = law_type:law_multicultural
			annex = ROOT
		}
		
		ai_chance = { 
			base = 90 
			modifier = { trigger = { c:SRL ?= { is_player = yes } } add = -100 }
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2					# No
		default_option = yes
		
		#Attack TAE
		c:SRL ?= {
			create_diplomatic_play = {
				name = TAE_SRL_war
				type = dp_annex_war_historical
				target_country = c:TAE
				
				add_war_goal = {
					holder = c:TAE
					type = humiliation
					target_country = c:SRL
				}
			}
		}
		
		ai_chance = {
			base = 10 
			modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 }
			modifier = { trigger = { c:SRL ?= { is_player = yes } } add = 100 }
		}
		
    }

}

# The Great Seal Bug
historical_diplomatic_incidents.32 = {
    type = country_event
	placement = c:SOV

    title = USA_SOV_Great_Seal_Bug.t 							# Title of the event; ADD LOCALISATION
    desc = USA_SOV_Great_Seal_Bug.d 							# Description of the event; ADD LOCALISATION
    #flavor = USA_SOV_Great_Seal_Bug.f 							# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/middleeast_police_breaking_door.bk2" }

    icon = "gfx/interface/icons/invention_icons/secret_police.dds" 	# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 		# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:USA ?= this											# Change TAG of the overlord ****
		game_date >= 1960.5.26									# START DATE: Date colony became self governing ****
		game_date < 1970.5.26									# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_democracy = yes									# Correct gov types
		is_sufficiently_independent = yes						# is_vassal = no		
		is_at_war = no											# war = no
		
		#Cause of incident
		exists = c:SOV
		c:SOV ?= {
			is_a_communist_government = yes
			is_sufficiently_independent = yes					# is_vassal = no
			is_at_war = no										# war = no
			NOT = { is_subject_of = ROOT }
		}
		NOT = { is_subject_of = c:SOV }
		has_diplomatic_pact = { who = c:SOV type = rivalry }
		c:SOV ?= { has_diplomatic_pact = { who = ROOT type = conduct_hostile_espionage is_initiator = yes } }
		
		# Event has not been fired before
		NOT = { has_variable = USA_SOV_Great_Seal_Bug_hdi }		# CHANGE ****
		
    }
	
	immediate = {
	
		# Fire only once
		set_variable = USA_SOV_Great_Seal_Bug_hdi				# Change variable ****
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#Damage relations
		change_relations = { country = c:SOV value = -50 }
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2					# No
		default_option = yes
		
		#Major discontent
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_armed_forces
		}
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_intelligentsia
		}
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_petty_bourgeoisie
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# Iranian Coup of 1953 - USA - Choice 1
historical_diplomatic_incidents.33 = {
    type = country_event
	placement = c:PER

    title = PER_1953_revolution.t 								# Title of the event; ADD LOCALISATION
    desc = PER_1953_revolution.d 								# Description of the event; ADD LOCALISATION
    #flavor = PER_1953_revolution.f 							# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/europenorthamerica_political_extremism.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 	# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 		# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Triggered by event
		
    }
	
	immediate = {
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1				# Yes
        highlighted_option = yes
		
		#Enforce regime change
		c:PER ?= {
		
			#Enact new regime
			effect_starting_politics_absolute_monarchy = yes
			effect_starting_politics_dictatorship = yes
			effect_conservative_ruling_coalition = yes
			
			ruler ?= { exile_character_with_martyrdom = yes }
			
			#Install new ruler
			if = {
				limit = { any_scope_character = { has_variable = mohammad_reza_pahlavi } }
				random_scope_character = {
					limit = { has_variable = mohammad_reza_pahlavi }
					set_character_as_ruler = yes
				}
			}
			else = {
				create_character = {
					first_name = "mohammad_reza_pahlavi_first_name"
					last_name = "mohammad_reza_pahlavi_last_name"
					ruler = yes ig_leader = yes historical = yes
					birth_date = 1919.10.26
					interest_group = ig_landowners
					ideology = ideology_autocratic
					culture = cu:persian
					religion = rel:shiite
					traits = {
						imperious
						reserved
						demagogue
					}
					dna = dna_reza_pahlavi
					on_created = { set_variable = mohammad_reza_pahlavi }
				}
			}
			
			generate_monarch_for_country = yes
			
			create_character = {
				first_name = "Shahnaz_Pahlavi_first_name"
				last_name = "Shahnaz_Pahlavi_last_name"
				heir = yes historical = yes female = yes
				birth_date = 1940.10.27
				interest_group = ig_industrialists
				ideology = ideology_autocratic
				culture = cu:persian
				religion = rel:shiite
				traits = {
					tactful
					expensive_tastes
					innovative
				}
				on_created = {
					set_variable = Shahnaz_Pahlavi
				}
			}
			
		}
		
		#International relations
		create_incident = {
			country = c:PER
			value = 10
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2				# No
		default_option = yes
		
		#Government Collapses
		c:PER ?= {
			
			#Becomes independent
			make_independent = yes
			
			#Enact new regime
			effect_starting_politics_parliamentary_republic = yes
			effect_starting_politics_dictatorship = yes
			
			#Install new ruler
			if = {
				limit = { any_scope_character = { has_variable = Mohammad_Mosaddegh } }
				random_scope_character = {
					limit = { has_variable = Mohammad_Mosaddegh }
					set_character_as_ruler = yes
				}
			}
			else = {
				create_character = {
					first_name = "Mohammad_Mosaddegh_first_name"
					last_name = "Mohammad_Mosaddegh_last_name"
					ruler = yes ig_leader = yes historical = yes
					birth_date = 1882.6.16
					interest_group = ig_trade_unions
					ideology = ideology_democratic
					culture = cu:persian
					religion = rel:shiite
					traits = {
						tactful
						innovative
						honorable
					}
					on_created = { set_variable = Mohammad_Mosaddegh }
				}
			}
			
		}
		
		#Major discontent
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_armed_forces
		}
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_intelligentsia
		}
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_industrialists
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# Iranian Coup of 1953 - USA - Choice 2
historical_diplomatic_incidents.34 = {
    type = country_event
	placement = c:PER

    title = PER_1953_revolution.t 								# Title of the event; ADD LOCALISATION
    desc = PER_1953_revolution.d 								# Description of the event; ADD LOCALISATION
    #flavor = PER_1953_revolution.f 							# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/europenorthamerica_political_extremism.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 	# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 		# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Triggered by event
		
    }
	
	immediate = {
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1				# Yes
        highlighted_option = yes
		
		#PER declares independence
		c:PER ?= {
			create_diplomatic_play = {
				type = dp_independence
				target_country = ROOT
				add_war_goal = {
					holder = ROOT
					target_country = c:PER
					type = regime_change
				}
				add_war_goal = {
					holder = c:PER
					target_country = ROOT
					type = humiliation
				}
			}
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2				# No
		default_option = yes
		
		#Major discontent
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_armed_forces
		}
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_intelligentsia
		}
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_industrialists
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# 1961 Iraq-Kuwait Showdown - GBR
historical_diplomatic_incidents.35 = {
    type = country_event
	placement = c:KUW

    title = Operation_Vantage.t 								# Title of the event; ADD LOCALISATION
    desc = Operation_Vantage.d 									# Description of the event; ADD LOCALISATION
    #flavor = Operation_Vantage.f 								# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/middleeast_battlefield_trenches.bk2" }

    icon = "gfx/interface/icons/event_icons/bicorne_hat.dds" 		# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 	# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Triggered by event
		
    }
	
	immediate = {
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1				# Yes
        highlighted_option = yes
		
		#International Community likes this
		change_infamy = -5
		every_country = {
			limit = { OR = { c:NEJ ?= this c:KUW ?= this c:BHR ?= this c:QAT ?= this c:UAE ?= this c:YEM ?= this c:NYE ?= this c:SYE ?= this c:PER ?= this c:JOR ?= this c:SYR ?= this } }
			change_relations = { country = ROOT value = 50 }
		}
		
		#Choice
		c:IRQ ?= { trigger_event = { id = historical_diplomatic_incidents.36 } }
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2				# No
		default_option = yes
		
		#Early Gulf War
		c:IRQ ?= {
			create_diplomatic_play = {
				name = iraq_kuwait_war
				type = dp_annex_war_historical
				target_country = c:KUW
				
				#war = yes
			}
		}
		
		#Major discontent
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_armed_forces
		}
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_intelligentsia
		}
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_industrialists
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# 1961 Iraq-Kuwait Showdown - IRQ
historical_diplomatic_incidents.36 = {
    type = country_event
	placement = c:KUW

    title = Operation_Vantage_1.t 								# Title of the event; ADD LOCALISATION
    desc = Operation_Vantage.d 									# Description of the event; ADD LOCALISATION
    #flavor = Operation_Vantage.f 								# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/middleeast_battlefield_trenches.bk2" }

    icon = "gfx/interface/icons/event_icons/bicorne_hat.dds" 		# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 	# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Triggered by event
		
    }
	
	immediate = {
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1				# Yes
        highlighted_option = yes
		
		#Major discontent
		add_radicals = {
			value = very_large_radicals
			interest_group = ig:ig_armed_forces
		}
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_rural_folk
		}
		add_modifier = { 
			name = national_humiliation 
			years = 2
			is_decaying = yes 
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2				# No
		default_option = yes
		
		#Early Gulf War
		c:IRQ ?= {
			create_diplomatic_play = {
				name = iraq_kuwait_war
				type = dp_annex_war_historical
				target_country = c:KUW
				
				war = yes
				
				add_target_backers = {
					c:GBR
				}
			}
		}
		
		#International reaction
		every_country = {
			limit = { OR = { c:NEJ ?= this c:KUW ?= this c:BHR ?= this c:QAT ?= this c:UAE ?= this c:YEM ?= this c:NYE ?= this c:SYE ?= this c:PER ?= this c:JOR ?= this c:SYR ?= this } }
			change_relations = { country = root value = -100 }
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# Operation Caban, 1979
historical_diplomatic_incidents.37 = {
    type = country_event
	placement = c:CAR

    title = Operation_Caban.t 									# Title of the event; ADD LOCALISATION
    desc = Operation_Caban.d 									# Description of the event; ADD LOCALISATION
    #flavor = Operation_Caban.f 								# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/middleeast_battlefield_trenches.bk2" }

    icon = "gfx/interface/icons/event_icons/bicorne_hat.dds" 		# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 	# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {													# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:FRA ?= this											# Change TAG of the overlord ****
		game_date >= 1979.9.19									# START DATE: Date colony became self governing ****
		game_date < 1989.9.19									# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_republican_government = yes
		is_sufficiently_independent = yes
		is_at_war = no
		
		country_rank >= rank_value:great_power
		is_not_neutral = yes
		
		#Cause of unrest
		exists = c:CAR
		c:CAR ?= {
			is_a_regime_monarchy = yes							# Correct gov types
			is_sufficiently_independent = yes					# is_vassal = no		
			is_at_war = no										# war = no
			OR = { is_subject_of = ROOT is_subject = no }
		}
		NOT = { is_subject_of = c:CAR }
		
		# Event has not been fired before
		NOT = { has_variable = Operation_Caban_hdi }			# CHANGE ****
		
    }
	
	immediate = {
	
		# Fire only once
		set_variable = Operation_Caban_hdi						# Change variable ****
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1			# Yes
        highlighted_option = yes
		
		#CAR to react
		create_incident = {
			country = c:CAR
			value = 5
		}
		c:CAR ?= { trigger_event = { id = CAR_gov_changes.3 } }
		
		#Domestic reaction
		add_loyalists = {
			value = small_radicals
			interest_group = ig:ig_petty_bourgeoisie
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2					# No
		default_option = yes
		
		#Major discontent
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_armed_forces
		}
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_intelligentsia
		}
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_trade_unions
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# The Rainbow Warrior Incident - FRA
historical_diplomatic_incidents.38 = {
    type = country_event
	placement = c:NZL

    title = FRA_NZL_Rainbow_Warrior_Incident.t 							# Title of the event; ADD LOCALISATION
    desc = FRA_NZL_Rainbow_Warrior_Incident.d 							# Description of the event; ADD LOCALISATION
    #flavor = FRA_NZL_Rainbow_Warrior_Incident.f 						# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/unspecific_fire.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 	# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 		# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {															# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:FRA ?= this													# Change TAG of the overlord ****
		game_date >= 1985.7.10											# START DATE: Date colony became self governing ****
		game_date < 1995.7.10											# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_presidential_democracy = yes								# Correct gov types
		is_sufficiently_independent = yes								# is_vassal = no		
		is_at_war = no													# war = no
		
		#French nuclear programme
		enacting_any_law = no
		has_modifier = nuclear_capability
		
		#François Mitterrand
		ruler ?= { 
			character_is_autocratic = yes 
			interest_group = { is_interest_group_type = ig_trade_unions } 
		}
		
		#Cause
		exists = c:NZL
		c:NZL ?= { 														# Change TAG of the colony ****
			is_a_democracy = yes
			is_sufficiently_independent = yes
			is_at_war = no
			
			NOT = { is_direct_subject_of = ROOT }
		}
		NOT = { is_direct_subject_of = c:NZL }
		
		# Event has not been fired before
		NOT = { has_variable = FRA_NZL_Rainbow_Warrior_Incident_hdi }	# CHANGE ****
		
    }
	
	immediate = {
	
		# Fire only once
		set_variable = FRA_NZL_Rainbow_Warrior_Incident_hdi				# Change variable ****
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1					# Yes
        highlighted_option = yes
		
		#Choice for NZL
		c:NZL ?= {
			trigger_event = { id = historical_diplomatic_incidents.39 }
		}
		
		#Infamy
		change_infamy = 5
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2			# No
		default_option = yes
		
		#Ban WMD
		start_enactment = law_type:law_wmd_banned
		
		#Domestic effects
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_armed_forces
		}
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_industrialists
		}
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_intelligentsia
		}
		add_modifier = {
			name = weakened_government_authority
			years = 10
			is_decaying = yes
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# The Rainbow Warrior Incident - NZL
historical_diplomatic_incidents.39 = {
    type = country_event

    title = FRA_NZL_Rainbow_Warrior_Incident_1.t 							# Title of the event; ADD LOCALISATION
    desc = FRA_NZL_Rainbow_Warrior_Incident.d 							# Description of the event; ADD LOCALISATION
    #flavor = FRA_NZL_Rainbow_Warrior_Incident.f 						# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/unspecific_fire.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 	# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 		# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {															# What triggers the event, if using other events or journal entries leave empty
		
		#Triggered by event
		
    }
	
	immediate = {
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1					# Yes
        highlighted_option = yes
		
		# Lose relations with FRA
		change_relations = { 
			country = c:FRA
			value = -100
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2					# No
		default_option = yes
		
		#Domestic effects
		add_modifier = {
			name = domestic_unrest
			years = 5
			is_decaying = yes
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}

# The Donroe Doctrine
historical_diplomatic_incidents.40 = {
    type = country_event

    title = USA_Donroe_Doctrine_2026.t 									# Title of the event; ADD LOCALISATION
    desc = USA_Donroe_Doctrine_2026.d 									# Description of the event; ADD LOCALISATION
    #flavor = USA_Donroe_Doctrine_2026.f 								# Flavor for event; ADD LOCALISATION, IF REQUIRED

    event_image = { video = "gfx/event_pictures/africa_diplomats_negotiating.bk2" }

    icon = "gfx/interface/icons/invention_icons/military_drill.dds" 	# Icon that shows on the map or the outliner
    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear" 		# Sound effect if needed

    duration = 1 # Duration to fire if triggers are met
	
	trigger = {															# What triggers the event, if using other events or journal entries leave empty
		
		#Overlord conditions
		c:USA ?= this													# Change TAG of the overlord ****
		game_date >= 2025.1.8											# START DATE: Date colony became self governing ****
		game_date < 2035.1.8											# END DATE: All historical events must not fire 10 years after the START DATE ****
		
		is_a_presidential_democracy = yes								# Correct gov types
		is_sufficiently_independent = yes								# is_vassal = no		
		is_at_war = no													# war = no
		
		#Causes
		ruler ?= { has_variable = Donald_Trump }
		country_rank = rank_value:super_power
		NOT = { has_law_or_variant = law_type:law_offensive_war }
		
		# Event has not been fired before
		NOT = { has_variable = USA_Donroe_Doctrine_2026_hdi }			# CHANGE ****
		
    }
	
	immediate = {
	
		# Fire only once
		set_variable = USA_Donroe_Doctrine_2026_hdi						# Change variable ****
		
	}
    
    option = {
        name = historical_diplomatic_incidents.option.1					# Yes
        highlighted_option = yes
		
		#Gain jingoism
		if = {
			limit = { NOT = { has_law_or_variant = law_type:law_offensive_war } }
			activate_law = law_type:law_offensive_war
		}
		if = {
			limit = { NOT = { has_law_or_variant = law_type:law_offensive_espionage } }
			activate_law = law_type:law_offensive_espionage
		}
		
		#Obsessions
		if = {
			limit = { exists = c:GRN }
			set_secret_goal = {
				country = c:GRN
				secret_goal = conquer
			}		
		}
		if = {
			limit = { exists = c:VNZ }
			set_secret_goal = {
				country = c:VNZ
				secret_goal = conquer
			}		
		}
		if = {
			limit = { exists = c:PER }
			set_secret_goal = {
				country = c:PER
				secret_goal = conquer
			}
		}
		if = {
			limit = { exists = c:PNM }
			set_secret_goal = {
				country = c:PNM
				secret_goal = conquer
			}
		}
		
		ai_chance = { 
			base = 90 
		}
		
    }
	
	option = {
        name = historical_diplomatic_incidents.option.2			# No
		default_option = yes
		
		#Domestic effects
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_armed_forces
		}
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_industrialists
		}
		add_radicals = {
			value = large_radicals
			interest_group = ig:ig_rural_folk
		}
		
		ai_chance = { 
			base = 10 modifier = { trigger = { high_historical_ai_railroading = yes } add = -100 } modifier = { trigger = { medium_historical_ai_railroading = yes } add = 0 } modifier = { trigger = { low_historical_ai_railroading = yes } add = 20 } modifier = { trigger = { no_historical_ai_railroading = yes } add = 80 } 
		}
		
    }

}